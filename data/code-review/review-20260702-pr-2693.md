# Review of PR #2693

## Driving issue
- #2664: [#2651 impl] Docs: make undeploy.sh/undeploy.yml primary; mark deploy-all.sh --destroy legacy

## Acceptance criteria checklist
- [x] Docs reference the new entry points and the typed-account-ID guard — All three files now show `undeploy.sh` / `undeploy.yml` as primary, with clear mention of the 12-digit account-ID gate.
- [x] Survive-by-design list is accurate (incl. `github-app/*`) — All three files include `adp/*/github-app/*`; verified against `force-delete-secrets.sh` which protects this pattern.
- [x] Two tracks kept distinct (script vs workflow), not conflated — CLAUDE.md uses a clear table separating "Self-managed" vs "ADP-managed" tracks; deploy-quickstart.md and self-managed-deploy.md each reference the other track as a cross-reference without conflating them.
- [x] PR review confirms teardown reads correctly end-to-end — The teardown narrative flows logically: primary → legacy → survive-by-design → per-module (CLAUDE.md) or primary → legacy → survive-by-design (quickstart/self-managed).

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
- **deploy-quickstart.md**: The survive-by-design list consolidates to `adp/gh-app-*`, `adp/*/github-app/*` while CLAUDE.md and self-managed-deploy.md also separately list `adp/*/gh-app-*`. This is technically fine since deploy-quickstart.md is a condensed guide, but the pattern sets differ slightly across files. This is cosmetic — not a blocker — since all patterns are correct subsets of what's actually protected.

### LOW — follow-up candidates
- **deploy-quickstart.md** omits `--from` and `--skip` flags that are documented in the other two files. Since deploy-quickstart.md is intended as a quickstart (not a reference), this is arguably intentional brevity, but a one-liner mentioning their existence could help operators who hit partial failures.
- Minor style: CLAUDE.md's `force-delete-secrets.sh` description says "Protects gh-app-*, github-app/*, and terraform-state-*" — the actual script protects `adp-terraform-*` (not `terraform-state-*`). Pre-existing in the description column; the new diff only added `github-app/*` to what was already there.

## Recommendation
**APPROVE** — All acceptance criteria are met. The docs correctly reflect the actual implementations. No merge blockers found.
