# Review of PR #1172

## Driving issue
- #1150: [Sub of #615] sec/E: code-quality cleanups — arbitrary-sleep + useless-inner-function

## Acceptance criteria checklist
- [x] FastAPI handlers in `app.py` suppressed with citation to the decorator — 3 handlers annotated: `gateway_error_handler` (line 167), `health` (line 188), `ready` (line 192)
- [x] Each `arbitrary-sleep` site either refactored to polling or suppressed with reason — All 4 sites suppressed with clear reason (already proper polling loops or rate-limit pacing)
- [x] No test flakes introduced — comments only, zero behavioral changes
- [ ] Probe scan re-run: 7 ERRORs cleared — cannot verify until post-merge scan, but nosemgrep placement is correct (line above finding per semgrep docs)

## Findings (by confidence)

### HIGH — merge blockers
- **FIXED**: `modules/agent-factory/webhook-ingress/tests/helpers.py:89`, `tests/e2e/chat/helpers.py:290,328`, `modules/agent-context/images/ingestion/refresh-repos.py:662` — nosemgrep inline comments exceeded module line-length limits (100 chars). Fixed by moving comments to line above the suppressed code. Commit: `00b6d93`.

### MEDIUM — discuss before merge
- None

### LOW — follow-up candidates
- Pre-existing format debt in `tests/e2e/chat/helpers.py` and `modules/agent-context/images/ingestion/refresh-repos.py` (unrelated to this PR)

## Security Review
- No secrets, credentials, or API keys
- No new dependencies
- No code changes — comment additions only
- No OWASP concerns — zero runtime behavioral changes
- No prohibited files committed

## Recommendation
APPROVE — after fixing the line-length violations (done in commit `00b6d93`), all acceptance criteria are met or not-yet-verifiable (probe scan must run post-merge).
