# Review of PR #1522

## Driving issue
- #1521: bug(webhook): @agent-product missing from MENTION_TO_PERSONA — product persona exists but is not triggerable

## Acceptance criteria checklist
- [x] Add `"@agent-product": "product"` to `MENTION_TO_PERSONA` dict — line 38 of `intent_parser.py` in the diff
- [x] No substring collision with existing keys — verified programmatically: `@agent-product` is not a substring of any existing key and no existing key is a substring of it
- [x] No reordering of existing dict entries — confirmed: the new entry is inserted between `@agent-architect` and `@agent-malware-analysis-agent`, existing entries unchanged
- [x] Unit test: `@agent-product` body resolves to persona `product` — `test_mention_product` test added
- [x] Regression test: existing mentions still resolve unchanged — `test_all_mention_mappings` iterates every entry
- [x] No collision between `@agent-product` and `@agent-pm` — `test_mention_product_no_collision_with_pm` test added
- [x] No gateway/agent-worker rebuild — changes are webhook-ingress only (2 files in `webhook-ingress/lambda/github/`)
- [x] Tests pass: 33/33 pass
- [x] Linter clean: `ruff check` + `ruff format --check` pass

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `MENTION_TO_PERSONA` dict does not have a corresponding `"product": "product"` entry in `LABEL_TO_PERSONA`. This is consistent with existing behavior (some personas are mention-only, some are label-only, some are both). Not a bug in this PR, but worth noting for completeness.

## Security review
- No secrets, credentials, or tokens in the diff
- No new dependencies introduced
- No new permissions or attack surface
- No user input handled unsafely (the dict is a static mapping; the `in` check on `body` is pre-existing behavior)
- No OWASP Top 10 concerns

## Recommendation
APPROVE — the change is minimal (1 dict entry + 3 well-structured tests), satisfies all acceptance criteria from the issue, introduces no regressions, and passes all lint/test checks.
