# Review of PR #1644

## Driving issue
- #1642: [Neptune EPIC #1529] bug(eval): summary aggregation disagrees with raw results (search_semantic 0 vs 12; overall 113 vs 125)

## Acceptance criteria checklist
- [x] Summary/by_verb derive strictly from the same `results[]` array (single source of truth) — The fix makes `manual_review` orthogonal to `passed/failed` counting. Both report-level and per-verb tallies now use independent `if` for manual_review, then `if error / elif passed / else failed` chain.
- [x] `passed` count equals `sum(1 for r in results if r.passed)` — The self-consistency assertion at line 770-777 enforces this invariant.
- [x] Each by_verb bucket matches `[r for r in results if r.verb==v]` — The by_verb tallying uses the same independent logic structure as the report-level tally.
- [x] Self-consistency assertion at report time fails loudly on mismatch — Lines 770-777: raises `AssertionError` with diagnostic message if `report.passed != raw_passed or report.passed != by_verb_passed`.
- [x] Unit test with partially-passing verb with `manual_review=True` — `test_manual_review_with_passes_counted_correctly` constructs exactly this scenario (10 search_semantic results, all manual_review=True, 4 passed).

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `test_self_consistency_assertion_fires_on_mismatch` only verifies the *logic* of the assertion would catch a mismatch (by checking `report.passed != raw_passed`), but doesn't actually call `run_evaluation()` with a broken tally to trigger the real `AssertionError`. This is fine — it's hard to inject a bad tally into the real function without monkey-patching internals. The integration test (`test_run_evaluation_self_consistency`) exercises the real code path.
- `pass_rate` denominator changed from `total - manual_review` to `total`. This is a semantic change (pass rate was 54/250 before, now it's 125/325). The issue doesn't explicitly request this, but it's the correct fix — `manual_review` items have valid pass/fail scores so they should be in the denominator. The baseline report should be regenerated to reflect the new rate.

## Recommendation
APPROVE — all acceptance criteria from #1642 are satisfied. The fix is minimal and surgical: 2 files changed (23 additions + 12 deletions in run_eval.py, 300-line new test file). Logic is correct. Tests pass (5/5). Lint clean.
