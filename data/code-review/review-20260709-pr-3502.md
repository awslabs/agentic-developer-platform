# Review of PR #3502

## Driving issue
- #3501: [deploy fix] agent-runtime CodeBuild build times out on virgin account (60min limit, no cache)
- #3502: fix(codebuild): increase agent-runtime build timeout and compute for virgin deploys

## Acceptance criteria checklist
- [x] Increase `agent-runtime` CodeBuild project timeout from 60→90 minutes — **Line 19 in main.tf: `build_timeout = 90` confirmed in the `agent-runtime` project entry; Terraform plan shows `build_timeout = 60 -> 90`**
- [x] Upgrade compute from `BUILD_GENERAL1_MEDIUM` to `BUILD_GENERAL1_LARGE` — **Line 19 in main.tf: `compute_type = "BUILD_GENERAL1_LARGE"` confirmed; Terraform plan shows `compute_type = "BUILD_GENERAL1_MEDIUM" -> "BUILD_GENERAL1_LARGE"`**
- [x] Make `compute_type` per-project configurable (using `lookup()` like `build_timeout` already is) — **Line 77: `compute_type = lookup(each.value, "compute_type", "BUILD_GENERAL1_MEDIUM")` — follows the same pattern as line 62: `build_timeout = lookup(each.value, "build_timeout", 60)`**
- [x] `terraform validate` passes on `platform/infra/` — **Confirmed via CI: Terraform Plan comment on PR shows `:white_check_mark: Terraform Plan — platform` with successful plan output**
- [x] Only `platform/infra/modules/codebuild/main.tf` modified — **Confirmed: 1 file changed, 2 additions, 2 deletions**

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `grype-scan` project already had `build_timeout = 90` but still uses the default MEDIUM compute. If it also times out on virgin deploys, the same pattern (add `compute_type = "BUILD_GENERAL1_LARGE"`) is ready to use. Not a blocker — just noting the precedent is clean.

## Security Review
- No secrets, tokens, credentials, or sensitive values in the diff
- No new IAM permissions introduced
- No new network access or security group changes
- Change is infrastructure configuration only (CodeBuild compute/timeout tuning)
- The existing `AdministratorAccess` policy attachment (line 51) is pre-existing tech debt with a TODO comment — not introduced by this PR

## Terraform Plan Verification
The CI-generated Terraform plan confirms:
- `module.codebuild.aws_codebuild_project.main["agent-runtime"]` will be updated in-place
- `build_timeout = 60 -> 90` ✓
- `compute_type = "BUILD_GENERAL1_MEDIUM" -> "BUILD_GENERAL1_LARGE"` ✓
- No unexpected resource changes from this diff (other changes in the plan are tag additions from a separate tags module — unrelated)

## Code Quality
- The `lookup()` pattern for `compute_type` is consistent with the existing `build_timeout` pattern on line 62
- Default value `"BUILD_GENERAL1_MEDIUM"` preserves backward compatibility for all other projects
- The change is minimal and surgical — exactly 2 lines modified

## Recommendation
**APPROVE** — Clean, minimal fix. All acceptance criteria satisfied. No security issues. Terraform plan confirms expected infrastructure changes. The PR was already merged and the fix is correct.
