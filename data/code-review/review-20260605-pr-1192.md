# Review of PR #1192

## Driving issue
- #1152: [Sub of #615] sec/C1: rotate + untrack committed beads credential key (INCIDENT)

## Revised scope (from Comment 1)
The original issue described a full rotation workflow, but Comment 1 revised scope to a minimal close-out:
1. Untrack the file (`git rm --cached`)
2. Add to local `.gitignore`
3. Add to `.gitleaks.toml` allowlist
4. Update `.secrets.baseline`
5. No rotation (zero consumers), no history rewrite, no directory deletion

## Acceptance criteria checklist

### From Comment 1's "Required scope":
- [x] `git rm --cached modules/agent-factory/beads/.beads-credential-key` — file shows as deleted in diff (correct: `git rm --cached` removes from index)
- [x] `.beads-credential-key` added to `modules/agent-factory/beads/.gitignore` — confirmed, added under "# Runtime files" section
- [x] Path added to `.gitleaks.toml` paths allowlist — confirmed: `'''modules/agent-factory/beads/\.beads-credential-key'''` with inline comment
- [x] `.github/security/.secrets.baseline` updated — entry added with `is_secret: false`
- [x] No `@agent-X` mentions in PR body, commits, or branch name — confirmed none
- [x] No `agent_learning/*.md` files in diff — confirmed none
- [x] No history rewrite — confirmed, no filter-repo or filter-branch
- [x] No rotation (file contents unchanged on disk) — confirmed, diff only shows removal from index
- [x] PR description includes grep audit output showing zero runtime references — confirmed in PR body

### From Comment 1's "Hard constraints":
- [x] No rotation — no new credential generated
- [x] No history rewrite — not attempted
- [x] No @agent mentions — clean

### From Comment 1's "Verification":
- [x] After merge, `git ls-files modules/agent-factory/beads/.beads-credential-key` will be empty
- [x] File still exists on disk (local; not verifiable from diff but .gitignore + rm --cached confirms intent)
- [x] No credential files remain tracked (`git ls-files | grep -i credential` — diff only removes, doesn't add)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
- `.secrets.baseline` `hashed_secret` is all zeros (`0000000000000000000000000000000000000000`): This suggests the entry was manually crafted rather than generated by `detect-secrets scan --update`. Practically harmless since the file is being untracked and `is_secret: false` is set, but if CI runs detect-secrets it might regenerate the baseline with the real hash and show a diff. Low risk of CI failure.

### LOW — follow-up candidates
- The original issue suggested adding `**/*credential-key` to the ROOT `.gitignore` as a broader safety net. The PR only adds to the local beads `.gitignore`. Comment 1 didn't explicitly require the root pattern, so this is acceptable per revised scope. Consider a follow-up for defense in depth.
- The diff exposes the 47-byte key content in the removal hunk. This is inherent to `git rm --cached` (the content was already in history anyway). Not a new exposure.

## Recommendation
APPROVE — all acceptance criteria met, no merge blockers. The MEDIUM finding (fabricated hash) is non-blocking since the file won't be scanned after untracking.
