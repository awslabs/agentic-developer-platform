# Review of PR #1479

## Driving issue
- #1478: fix(agent-context): DeepWiki OOMs on large repos (>500MB) — process small repos first in backfill

## Acceptance criteria checklist
- [x] Sort repos by size ascending (smallest first) — `repos_to_process.sort(key=lambda r: repo_sizes[r])`
- [x] Skip repos > 600MB with log warning — `repos_to_skip` list + `log.warning(...)` with size, threshold
- [x] Skipped repos get `deepwiki_sha: "skip_too_large"` — `repo_state[repo]["deepwiki_sha"] = "skip_too_large"`
- [x] Reuse existing `_repo_size_mb()` helper — `repo_sizes[repo] = _repo_size_mb(repo)`, no redundant calls
- [x] State file format unchanged — uses existing `deepwiki_sha` field with string sentinel
- [x] Unit test: sort order — `TestBackfillSortOrder` (3 tests: ascending, same-size, zero-size)
- [x] Unit test: skip threshold — `TestBackfillSkipThreshold` (4 tests: above, exact boundary, just-above, all-too-large)
- [x] Unit test: state update — `TestBackfillSkipStateUpdate` (2 tests: sentinel set, sentinel excludes from future backfill)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
- **Test approach uses re-implementation rather than calling the actual function**: The tests re-declare `_backfill_sort_and_filter()` to test in isolation, rather than importing and testing the real `backfill_deepwiki_wikis()`. This is an established pattern in this test file (see lines 16-20: "Re-declare validators here to test them in isolation without importing the full refresh-repos.py which pulls requests, etc. and needs /app/"). The approach is acceptable given the import constraints, and the tests do verify the correct logic. LOW risk of drift since the logic is simple (list comprehension + sort).

### LOW — follow-up candidates
- The `_repo_size_mb()` function makes unauthenticated GitHub API calls (line 659-661). For repos in private orgs, this would return 0 (API gives 404 for private repos without auth). However, the current use case targets public repos, and 0-size repos are correctly sorted first (processed before others). No action needed now.
- Consider caching `_repo_size_mb()` results across runs in the state file to avoid redundant API calls on subsequent runs. Not needed for the current scope (4 repos).

## Verification
- ruff check: PASS (no errors in modified files)
- ruff format: PASS (files already formatted)
- pytest: PASS (63 tests, 0 failures)
- Must-not-commit check: PASS (only expected files modified)

## Recommendation
APPROVE — all acceptance criteria satisfied, no merge blockers, clean lint/tests.
