# Review of PR #1568

## Driving issue
- #1553: [Neptune EPIC #1529] ORCHESTRATOR: autonomously build+test all child stories to a verified working module

## Context
This PR removes the non-existent `@sourcegraph/scip-java@0.9.0` npm package from the ingestion Dockerfile. This package returns HTTP 404 on npm, blocking every ingestion image build since PR #1558 merged. The design doc (D7 in neptune-deep-graph-design.md) explicitly states "Java/Rust deferred — not in current corpus." This is the single blocker for Neptune population → done-gate.

## Acceptance criteria checklist
- [x] Removes `@sourcegraph/scip-java@0.9.0` from npm install line — confirmed removed at Dockerfile line 38
- [x] Removes JVM/Maven apt-get install layer (only needed for scip-java) — replaced with deferred comment
- [x] Updates Node.js comment to remove scip-java reference — Dockerfile line 29 comment fixed
- [x] Adds deferred note in Dockerfile — "JVM deferred — scip-java not in current corpus (D7)" comment added
- [x] Updates scip_indexer.py docstring — Java moved to "Deferred" section with correct note
- [x] Aligns with D7 design decision — D7 says "Java/Rust deferred", PR defers Java
- [x] No functional regression for supported languages — Python, TypeScript, Go indexers untouched
- [x] Image build should succeed (no 404 on npm install) — scip-java package reference removed

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge

- `scip_indexer.py:542-544`: The dispatch table still maps `java`, `kotlin`, `scala` to `_index_java()` function, and the `_index_java()` function (lines 467-487) still exists in the file. When Java repos are encountered, it will call `scip-java` binary → get `FileNotFoundError` → return error gracefully. This is acceptable behavior (fail-loud per design) and not a regression since scip-java was never installed successfully anyway. The function and dispatch entries serve as future stubs. **No action required.**

### LOW — follow-up candidates
- Future: when Java support is added, `scip-java` is a JVM binary (not an npm package). The docstring update in scip_indexer.py correctly notes "JVM binary, not npm". The Dockerfile comment also leaves breadcrumbs for future implementation.

## Security Review
- No secrets, API keys, tokens, or credentials in the diff
- No new dependencies added (only removed)
- No new network endpoints or attack surface
- Removing JDK/Maven layer reduces image attack surface
- No privilege escalation paths

## Recommendation
APPROVE — clean, minimal fix that removes a broken dependency per design doc. No regressions to supported languages.
