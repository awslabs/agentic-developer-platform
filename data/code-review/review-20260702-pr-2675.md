# Review of PR #2675

## Driving issue
- #2673: [deploy fix] Phase 6d: ensure first-admin Cognito custom:role/org_id is set (else admin UI hidden) — verify + self-heal

## Acceptance criteria checklist
- [x] Make bootstrap-admin add post-step verification that fails loudly if `custom:role` is unset after the run — **Satisfied**: Step 5 reads back both attributes via `admin-get-user` and calls `fail()` (exit 1) if they're empty, "None", or mismatched (role only).
- [x] Add Phase-6d verification probe to deploy runbook — **Satisfied**: `deploy-quickstart.md` now includes explicit `admin-get-user` commands with expected output and remediation guidance.
- [x] Document the ADP pipeline gap — **Satisfied**: "⚠️ Pipeline gap" callout explicitly states `gateway-deploy.yml` does NOT run `bootstrap-admin.sh` and describes consequences.
- [x] Idempotent re-run is safe — **Satisfied**: attributes already set → verification passes (no destructive operations). `admin-update-user-attributes` is a SET, not CREATE.
- [x] Partial run (DB only) FAILS the phase with a clear remediation message — **Satisfied**: if `admin-update-user-attributes` fails silently, the read-back detects absence and emits the full fix command.
- [x] `--dry-run` mode skips verification — **Satisfied**: explicit `if [ "$DRY_RUN" = true ]` guard around the verification block.
- [x] `bash -n` passes — **Verified locally**: syntax check passes.

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `bootstrap-admin.sh:152-154`: The verification checks `custom:org_id` for existence (empty/None) but does NOT verify it matches `$EFFECTIVE_ORG`. By contrast, `custom:role` IS value-matched against `$ROLE` (line 149). This inconsistency is minor — the primary failure mode (attribute never set) IS caught — but for completeness, an org mismatch check (analogous to lines 149-150) would be more robust. Suggested follow-up, not a blocker.

## Code quality notes
- Good use of `2>/dev/null || echo ""` to handle AWS CLI errors gracefully without triggering `set -e`.
- "None" check is correct — JMESPath returns literal "None" for empty attribute queries with `--output text`.
- `fail()` messages include the full remediation command, which is excellent UX for operators.
- The deploy-quickstart.md additions are well-structured with clear expected/actual guidance.
- No secrets, credentials, or injection risks introduced.

## Recommendation
**APPROVE** — All acceptance criteria from #2673 are satisfied. The code is correct, handles edge cases well, and the documentation additions are clear and actionable. The one minor inconsistency (missing org_id value check) is LOW severity and fine as a follow-up.
