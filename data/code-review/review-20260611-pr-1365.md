# Review of PR #1365

## Driving issue
- #1358: [Sub of #1345] Dual-rail SBOM generation (Syft source-for-all + image-for-Dockerfiles)

## PR Summary
- **Type:** Doc-only (design note + agent learnings)
- **Files:** 2 added, 0 modified
- **Additions:** 545 lines, 0 deletions

## Acceptance criteria checklist

- [x] Design covers dual-rail SBOM: source-for-all + image-for-Dockerfiles — §3.1 and §3.2 comprehensively detail both rails
- [x] Both rails use Syft with CycloneDX JSON output — §3.1 specifies `syft dir:` and §3.2 specifies `syft {image} -o cyclonedx-json`
- [x] Dependency rows written to Postgres — §4.2 defines the `dependencies` table schema with proper indexes
- [x] Cost guardrails specified — §8 lists SHA-gated rebuild, concurrency cap (5), per-build timeout (10 min), overall timeout (30 min)
- [x] Honest coverage reporting — §3.3 shows coverage query with `build_failed` markers, not silent gaps
- [x] Source SBOM runs in KEDA worker — §3.1 "Where it runs: Inside the existing KEDA ingestion worker pod"
- [x] Image SBOM runs on CodeBuild — §3.2 "Where it runs: CodeBuild (has Docker daemon)"
- [x] Validation plan present — §11 includes unit, integration, smoke, and regression test descriptions
- [x] Deployment section present — §12 specifies 7-step deployment sequence with rollback strategy
- [x] Fact-verification of design assumptions — §2 covers S3 Vectors, Mountpoint, OSV-Scanner, Trivy, PostgreSQL 16
- [x] Critical finding: OSV-Scanner cannot consume CycloneDX — §2.3 clearly documents this and recommends Trivy

## Findings (by confidence)

### HIGH — merge blockers

- `agent_learning/2026-06-11-issue-1358-learnings.md`: This file is committed to the PR but `agent_learning/` is listed in `.gitignore`. This is a "must not commit" violation — the file was force-added (bypassing gitignore) and should be removed from the PR. The content is valuable but belongs outside version control (it's gitignored for a reason — these are ephemeral agent learning files that should not clutter the main branch).

### MEDIUM — discuss before merge

- None identified. The design note is thorough, well-structured, and aligns with both the issue requirements and the existing codebase patterns described in the architect's detailed issue comment (#12).

### LOW — follow-up candidates

- §10 Q5 pins Syft at v1.11.1 (matching existing `bs-syft-scan.yml`), but notes in the learnings file that latest is v1.45.1. A version bump decision should be tracked separately.
- §4.2 defines a `vulnerabilities` table — this arguably belongs in the vuln-matching sub-issue (#1359 scope), not here. Not a blocker since it's forward-looking schema in a design doc, but the implementing agent should only create `repositories` + `dependencies` tables for this issue.
- The `UNIQUE (repo_id, package_url, source, COALESCE(dockerfile_path, ''))` constraint in §4.2 uses a function expression — in PostgreSQL this requires a UNIQUE INDEX rather than a table-level UNIQUE constraint. The implementing agent should use `CREATE UNIQUE INDEX` instead.

## Recommendation
**REQUEST CHANGES** — remove `agent_learning/2026-06-11-issue-1358-learnings.md` from the PR (it's in `.gitignore`). After that fix, APPROVE.
