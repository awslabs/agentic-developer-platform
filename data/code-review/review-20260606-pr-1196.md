# Review of PR #1196

## Driving issue
- #1157: [Sub of #615] sec/H2: enable RDS TLS verification (database.py + alembic env)

## Acceptance criteria checklist
- [x] Add `rds_tls_verify: bool = True` to `config.py` — `config.py:15`
- [x] In `database.py`: stop overriding `ssl.create_default_context()` defaults — lines 135-140
- [x] In `database.py`: log warning when verification disabled — `logger.warning(...)` on line 137
- [x] In `alembic/env.py`: same pattern via `os.getenv("BG_RDS_TLS_VERIFY", "true")` — lines 102-107
- [x] Tests: default (unset) → CERT_REQUIRED + check_hostname=True — `test_tls_verification_enabled_by_default`
- [x] Tests: explicit "true" → CERT_REQUIRED + check_hostname=True — `test_tls_verification_enabled_when_explicitly_true`
- [x] Tests: "false" → CERT_NONE + check_hostname=False — `test_tls_verification_disabled_when_false`
- [x] Tests: "False" (capital F) → case insensitive opt-out — `test_tls_verification_disabled_case_insensitive`
- [x] Tests: warning log emitted on opt-out — `test_tls_disabled_emits_warning`
- [x] No Dockerfile changes — file list confirms only 4 Python files
- [x] No workflow changes — no .github/ files in diff
- [x] No Terraform changes — no .tf files in diff
- [x] No `agent_learning/*.md` files committed — confirmed via `gh pr diff --name-only`
- [x] Env-var naming consistent: `BG_RDS_TLS_VERIFY` maps to `Settings.rds_tls_verify` via `env_prefix = "BG_"` in model_config
- [x] Linting passes: `ruff check` + `ruff format --check` both clean
- [x] New tests pass: 5/5 PASSED

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
1. `alembic/env.py` could log a warning when TLS verification is disabled (like `database.py` does). Minor inconsistency but acceptable since alembic env has no logger configured in the same way. Not a blocker.
2. Follow-up: add `rds.force_ssl = 1` to the RDS parameter group for server-side enforcement (architect flagged as separate issue).
3. Follow-up: pin `ca_cert_identifier = "rds-ca-rsa2048-g1"` in Terraform (explicit > implicit).

## Security assessment
- No secrets or credentials in the diff
- No new dependencies introduced
- The change strengthens security by default (CERT_REQUIRED replaces CERT_NONE)
- Emergency escape hatch (`BG_RDS_TLS_VERIFY=false`) logs a clear warning — appropriate for operational recovery
- No injection risk — env var is compared against string literal "false"
- No SSRF, no path traversal, no XSS vectors

## Recommendation
APPROVE — all acceptance criteria satisfied, no merge blockers. Clean, minimal, well-tested change.
