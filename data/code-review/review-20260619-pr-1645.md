# Review of PR #1645

## Driving issue
- #1643: [Neptune EPIC #1529] understand scores 15% — NL-question vs structural-target contract mismatch (decide re-scope vs NL-resolution)

## Acceptance criteria checklist
- [x] Architecture decision made (option 1: re-scope golden dataset) — architect decision posted in issue comment #5/#6; verb-contracts.md documents the contract explicitly
- [x] `understand` verb contract documented explicitly — `modules/agent-context/docs/verb-contracts.md` created (149 lines), covers all 5 verbs with valid/invalid input examples and routing table
- [x] NL questions re-scoped — 25/65 understand entries rewritten from NL prose to structural targets (file paths, directories, symbol references)
- [x] Remaining golden questions use structural targets — all 65 entries now contain `/` or `::` (validated programmatically)
- [x] Total question count preserved — 325 questions, same as main
- [x] Per-verb counts preserved — understand:65, search_exact:75, search_semantic:75, browse:75, impact:35 (identical to main)
- [x] All IDs preserved — same 325 unique IDs, no additions/removals
- [x] YAML validates — `yaml.safe_load()` succeeds without errors
- [x] Notes field traces original NL query and destination verb — 23 → search_semantic, 2 → browse
- [x] README.md updated with verb-contract quick-reference — table added to eval README (20 lines)

## Findings (by confidence)

### HIGH — merge blockers
None found.

### MEDIUM — discuss before merge
None found.

### LOW — follow-up candidates
- The 25 NL questions are documented as "moved to search_semantic" in notes, but they haven't been actually added to the search_semantic section of golden.yaml. This is intentional per the architect's plan (search_semantic isn't implemented yet), but a follow-up issue should track adding those entries when the verb is ready.
- Some key_facts in rewritten entries are generic single words (e.g., `["cookie", "extract", "browser"]`). These may be overly permissive for scoring — could match on partial/tangential content. Not a blocker since the current scoring function just checks for presence in the response text.

## Validation performed
1. YAML parse: `yaml.safe_load()` — success
2. Question count: 325 (matches main)
3. Verb counts: identical to main across all 5 verbs
4. ID uniqueness: all 325 IDs unique, no added/removed IDs
5. Structural target validation: 0/65 understand entries missing `/` or `::`
6. Repo-prefix validation: all 25 rewritten queries start with their repo name
7. Contract alignment: verb-contracts.md accurately reflects `_parse_target()` (line 863), `server.py:51` tool description, and Neptune design doc D15
8. No Python code changed — only .md and .yaml files; lint/test not applicable

## Recommendation
**APPROVE** — Dataset-only change that correctly implements the architecture decision. No code changes, no risk of regression. The verb contract documentation is accurate and well-structured.
