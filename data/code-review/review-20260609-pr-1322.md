# Review of PR #1322

## Driving issue
- #1319: [Sub of #1287] Design: resolve GitHub sender → cognito_sub for Personal Context (+ risk register)

## PR Summary
- **Title:** design(identity): GitHub sender → cognito_sub resolution (#1319)
- **Files:** 1 added (`docs/design-1319-github-sender-to-cognito-sub.md`, +340 lines)
- **Type:** Design document only — no code changes

## Acceptance criteria checklist

### Output shape (5 deliverables from issue):
- [x] **Recommended resolution design (where, how, exact lookup)** — Section 2 fully covers: where (webhook-ingress Lambda via existing gateway endpoint), how (extend `POST /internal/v1/resolve-user`), exact lookup (two-hop: DDB → gateway → Postgres `users.cognito_sub`), plus code-level change descriptions with pseudocode
- [x] **Fail-closed + bot/service-account + chained-run handling** — Section 3 (fail-closed contract with invariant + 6-row failure mode table + 3-layer structural enforcement) and Section 4 (bots: no PC; chained runs: propagate root_human_id → cognito_sub; 4-row decision table)
- [x] **Risk register (expanded, severity + mitigation)** — Section 6: 10 risks (exceeds the 8 minimum required), each with Severity, Likelihood, Impact, and Mitigation columns
- [x] **Consistency guarantee (one owner_sub per human)** — Section 5: explains why both paths resolve to same value, uniqueness enforcement, dual-store detection mechanism
- [x] **Implementation issues to file (scoped for developer)** — Section 7: Issue A (core fix, 7 changes, 6 test cases, deployment order), Issue B (e2e assertion for #1295 with 5 assertions), Issue C (observability metrics in CloudWatch namespace)

### "What to design" items (5 from issue):
- [x] Where the resolution happens — Section 2.1 (in webhook-ingress Lambda at sender-resolution time, via existing gateway call)
- [x] The lookup mechanism — Section 2.2 (two-hop via Postgres; alternatives table with 4 options and rationale for rejection)
- [x] Fail-closed contract — Section 3 (core invariant, enumerated failure modes, 3-layer defense in depth)
- [x] Bot/app/service-account senders — Section 4.1 (no personal context) + 4.2 (chained runs propagate root human) + 4.3 (summary decision table)
- [x] Consistency with the chat path — Section 5 (one owner_sub, same Cognito sub from both paths, detection query for regression)

### Required risks (8 minimum, all must be addressed):
- [x] Mis-resolution → cross-user attribution — R1 (Critical, Very Low, 4 mitigations)
- [x] Spoofable sender — R2 (Critical, Very Low, 5 mitigations)
- [x] Unlinked / partial-coverage tenants — R3 (Medium, Medium, 4 mitigations)
- [x] Bot sender writing personal context — R4 (High, Very Low, 4 mitigations)
- [x] Chained-run identity drift — R5 (Medium, Low, 4 mitigations)
- [x] Latency / failure of the lookup in hot path — R6 (Medium, Low, 5 mitigations)
- [x] PII / identity data exposure — R7 (Medium, Low, 6 mitigations)
- [x] Dual-store split (regression) — R8 (High, Low, 4 mitigations)
- [x] Webhook secret compromise — R9 (Critical, Very Low, 5 mitigations) [BONUS]
- [x] Race condition during identity linking — R10 (Low, Very Low, 3 mitigations) [BONUS]

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- L1: Section 10 (Open Questions) raises 3 questions for maintainer — these are appropriate design-stage questions and do not block the design doc from merging. They should be addressed before filing implementation issues.
- L2: The document includes pseudocode snippets (Sections 2.2 items 1-4) which are helpful for the developer but should be validated against the actual codebase during implementation. Not a blocker for a design doc.

## CI Status
- detect-secrets: ✅ pass
- Bandit (Python): ✅ pass
- Checkov (IaC): ✅ pass
- cfn-nag: ✅ pass
- npm audits: ✅ pass
- Grype/Syft/Semgrep: pending (won't flag a markdown file)

## Must-not-commit check
- No `agent_learning/*.md` files committed ✅
- No `.terraform/` files ✅
- No `tfplan` files ✅
- No `.env` or credentials files ✅
- File follows existing naming convention (`docs/design-{issue}-{slug}.md`) matching `docs/design-719-multi-user-tenant-matching.md` ✅

## Security Review
- No secrets, API keys, tokens, or credentials in the document ✅
- No executable commands that could be confused as instructions ✅
- Pseudocode is clearly illustrative, not runnable ✅
- `cognito_sub` values in the doc are described generically ("opaque UUID") — no real user identifiers ✅
- The risk register itself properly highlights security concerns (R1, R2, R9) ✅

## Recommendation
**APPROVE** — All acceptance criteria satisfied. Design is thorough, well-structured, and addresses every deliverable and risk from the issue specification. No code changes means no lint/test/format checks needed.
