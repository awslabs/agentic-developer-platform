# Review of PR #1186

## Driving issue
- #1162: [Sub of #615] sec/H7: gh issue create command injection from LLM-influenced strings

## Acceptance criteria checklist
- [x] Site refactored to use `execFile` with argv array (no shell) — `createSubIssue()` now uses `execFileCommand('gh', args)` with `execFileSync` default (line 339). No shell invocation.
- [x] Input validators applied: `validateIssueTitle()` (max 256 chars, no leading dash, no null bytes), `validateIssueBody()` (max 65536 chars, no null bytes), `validateLabel()` (charset allowlist, no leading dash) — lines 64-106.
- [x] Body uses `--body-file` temp file pattern (mirrors `ghPost.ts`) — line 324-331. Body content never enters argv or shell.
- [x] Labels passed as individual `--label` flags (not comma-joined in shell-quoted string) — line 333-335.
- [x] Red-team test: title `"; whoami #"` does NOT execute (test line 74-80, verified via mock interceptor)
- [x] Temp file cleanup in `finally` block (line 363-364) — cleanup is best-effort, handles errors gracefully.
- [x] `configureSubIssues` interface backward-compatible — `execFileCommand` is optional, defaults to `execFileSync`.
- [x] Regression tests pass — `agent-pm-sub-issues.test.ts` (17/17 pass, unaffected).
- [x] TypeScript compiles cleanly (`tsc --noEmit` passes).

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
1. **GraphQL injection surface** (lines 152, 186, 235, 262, 377): `owner`/`repo`/`issueNumber` are interpolated into GraphQL query strings via `execCommand` (shell string). These are environment-derived (not LLM-influenced) but should be hardened in #1149's broader sweep. The fix correctly scopes to the confirmed LLM-influenced `createSubIssue` site only.
2. **Temp file permissions**: `/tmp/sub-issue-body-*.md` created with default umask. In single-tenant agent pods this is fine. If the runner ever becomes multi-tenant, a `mode: 0o600` option on `writeFileSync` would be defense-in-depth. Very low priority.
3. **`@agent-X` mention-stripping**: Deferred per architect review — separate issue needed (routing-injection, different severity class). Agreed.

## Security Review

### Findings
- **No secrets or credentials** in the diff
- **No new dependencies** introduced
- **Injection fix verified**: shell-string `execCommand` replaced with argv-based `execFileCommand` for the `gh issue create` call. Shell metacharacters (`$(...)`, backticks, `;`, `&&`) are now inert.
- **Temp file race condition**: mitigated by random suffix (`Date.now()` + `Math.random().toString(36).slice(2)`). Acceptable for single-tenant pod.
- **Remaining GraphQL sites**: not in scope (env-var-derived inputs, not LLM-influenced). Tracked by #1149.

### Verdict
No security concerns. The fix correctly eliminates the confirmed command injection vector.

## Recommendation
**APPROVE** — all acceptance criteria satisfied, no merge blockers, tests pass, security review clean.
