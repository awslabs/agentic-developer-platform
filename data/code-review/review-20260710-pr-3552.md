# Review of PR #3552

## Driving issue
- #3550: chore(github): delete legacy create-github-apps.sh, add registration-time permission validation

## Acceptance criteria checklist
- [x] Delete `platform/scripts/create-github-apps.sh` — file removed (475 lines deleted)
- [x] Add post-registration permission/event validation to `register-github-app.sh` — Step 7b added (128 lines): mints JWT via openssl, calls GET /app, diffs permissions + events against expected sets mirrored from `_build_app_manifest()`, emits warnings only
- [x] Remove stale references from CLAUDE.md — replaced with `register-github-app.sh` entry
- [x] Remove stale references from SETUP-GUIDE.md — added note that legacy script removed, directs to register-github-app.sh
- [x] Remove stale references from README.md — replaced 3-App section with single-App description
- [x] Remove stale references from self-managed-deploy.md — replaced script table entry
- [x] Update BYO doc §4 to present tense — "once #3354 ships" → "Preferred: the Connections UI"; added CLI fallback section
- [x] Update BYO doc §2.2 to cite manifest as authoritative — added callout block
- [x] Zero behavioral change to deployed systems — correct, only comments/docs/validation (warning-only) changed
- [x] `grep -r "create-github-apps"` returns only historical/design-note references — verified: remaining hits are in design-notes (analytical), epic planning (historical), deploy docs (historical context), and the ARC-specific `runner-infra/scripts/create-github-apps.sh` (separate script, not deleted)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `runner-infra/scripts/create-github-apps.sh` (524 lines) still exists as a separate ARC-path script. The issue specifically targets only `platform/scripts/create-github-apps.sh` so this is correct, but it may be confusing to have a same-named script elsewhere. Future cleanup candidate.
- The validation function uses `grep -o` for JSON parsing which is fragile for edge cases (e.g., permission values containing quotes). However, the issue specifies "warnings only — never fails registration" and the function returns 0 on any parsing failure, so this is acceptable for a best-effort check.

## Recommendation
APPROVE — all acceptance criteria satisfied, no merge blockers, surgical changes only.
