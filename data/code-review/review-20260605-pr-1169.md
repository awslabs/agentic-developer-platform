# Review of PR #1169

## Driving issue
- #1146: [Sub of #615] sec/A: gitleaks allowlist for fake credentials in test fixtures (~56 findings)

## Acceptance criteria checklist
- [x] Per-file audit completed; `data/security/.../audit.md` lists each file + confirmation — audit doc present at `data/security/2026-06-05-class-A-gitleaks-allowlist-audit.md` with 18 files individually verified
- [x] `.gitleaks.toml` lands with explicit path list (no blanket `**/tests/**`) — confirmed: 19 explicit paths (18 test files + audit doc), no globs
- [ ] Probe scan re-run shows Class A ERRORs = 0 — post-merge validation step, cannot verify at review time
- [ ] Negative test: planted leak in `src/` is still caught — developer documented this in PR description with evidence; verified claim is consistent

## Findings (by confidence)

### HIGH — merge blockers
- `agent_learning/2026-06-05-issue-1146-learnings.md`: **MUST NOT be committed.** AGENTS.md line states "Never commit `agent_learning/*.md`" and the directory is listed in `.gitignore` (line 61). The file was force-added bypassing the gitignore. This commit must be removed from the PR.

### MEDIUM — discuss before merge
- `.gitleaks.toml` uses `[allowlist]` (singular) instead of `[[allowlists]]` (plural array) from the issue design. The developer notes this is the correct format for gitleaks v8.18+, where the plural form is silently ignored. **Verified this is correct** — gitleaks v8 documentation confirms `[allowlist]` for global allowlist configuration. This is a justified deviation.
- The audit doc itself (`data/security/2026-06-05-class-A-gitleaks-allowlist-audit.md`) is added to the path allowlist (line 19 of `.gitleaks.toml`). This is reasonable since the doc quotes the synthetic credentials for documentation purposes, but adds a file outside `**/tests/**` to the allowlist.

### LOW — follow-up candidates
- The `data/security/` directory doesn't exist on `main` — it will be created by this PR. This is fine (intentional new directory for audit records).
- WIP commit message in first commit ("WIP: agent/developer starting #1146") — will be squash-merged away, so no action needed.
- Gitleaks is not currently in the CI pipeline (only referenced in manual review docs). The issue references "gitleaks CI step on the PR" in validation, but there's no such step configured yet. This is pre-existing — the allowlist is still correct and will work once gitleaks is added to CI.

## Recommendation
**REQUEST CHANGES** — remove `agent_learning/2026-06-05-issue-1146-learnings.md` from the PR, then APPROVE.

Reviewer will fix this directly on the branch since it's a simple removal.
