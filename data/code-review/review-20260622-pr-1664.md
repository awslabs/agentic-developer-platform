# Review of PR #1664

## Driving issue
- #1661: bug(lineage): parent_invocation_id never populates — correlation-pointer key-format mismatch (worker vs webhook)

## Approved Design (Comment #9 by @PranavSharma1000)
The approved build spec prescribes exactly TWO fixes, both in the agent-worker, so that the correlation pointer written by the worker matches what the webhook reads.

## Acceptance criteria checklist

- [x] **Fix 1 — channel-key format**: Worker builds issue key as `github:repo={repo},issue={N}` — VERIFIED at `lib/correlation_store.py:40` (`f"{provider}:repo={repo},{kind}={number}"`) and `entrypoint.py:984-985` (`channel_key("github", repo, "issue", issue_number)`)
- [x] **Fix 2 — DDB attribute names**: Worker writes `correlation_id` / `root_human_id` / `is_human_rooted` (no `latest_` prefix) — VERIFIED at `lib/correlation_store.py:71-73`
- [x] **PR path unchanged**: Legacy `f"github:{repo}:{channel_suffix}"` preserved for PR pointers — VERIFIED at `entrypoint.py:988`
- [x] **Key-parity test**: Pinned-string test asserting `channel_key("github","aws-e/adp","issue",1320)` == `"github:repo=aws-e/adp,issue=1320"` — VERIFIED in `TestChannelKey.test_parity_with_webhook_format` and `test_issue_key_format`
- [x] **Attribute-name tests**: Tests assert written item uses `correlation_id` not `latest_correlation_id`, etc. — VERIFIED in `TestWritePointer.test_writes_item_to_dynamodb` (lines 88-95) with explicit `"latest_*" not in item` assertions
- [x] **Round-trip unit test**: Worker write produces item parseable by webhook's `read_pointer` — VERIFIED in `TestRoundTripCompatibility.test_worker_item_has_webhook_compatible_attributes`
- [x] **Issue key format in entrypoint**: `_write_outbound_correlation("aws-e/adp", "issue:1320", ...)` passes canonical `channel_key="github:repo=aws-e/adp,issue=1320"` — VERIFIED in `test_issue_key_uses_canonical_format`
- [x] **No webhook changes**: Only worker files modified — VERIFIED (5 files, all under `agent-worker-image/`)
- [x] **No backfill, no chain board (#1662)**: Not touched — VERIFIED
- [x] **Lint/format clean**: `ruff check` all pass, `ruff format --check` all formatted
- [x] **Tests pass**: 18 worker correlation tests pass, 10 webhook common tests pass (no regression)

## Invariant checks (from approved design "OUT OF SCOPE" section)

- [x] PR-pointer `pr:{branch}` keying NOT touched — confirmed `else` branch at entrypoint.py:987-988
- [x] No webhook-ingress files modified — confirmed diff is 5 files all in `agent-worker-image/`
- [x] No historical backfill attempted — confirmed
- [x] No chain board (#1662) changes — confirmed

## "Must not commit" checks

- [x] No `agent_learning/*.md` files — CLEAN
- [x] No `.terraform/` files — CLEAN
- [x] No `tfplan` files — CLEAN
- [x] No secrets, credentials, or `.env` files — CLEAN

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The docstring for `write_pointer` at line 54 was updated to show the new format in the example, but the function signature param still uses the generic name `channel_key: str` (this is fine — just noting thoroughness of the update).
- The WIP commit (`c3d772b`) could be squashed, but this is cosmetic for a feature branch that will be squash-merged.

## Test results
- `tests/test_correlation_store.py`: 8 passed
- `tests/test_correlation_store_parent.py`: 5 passed  
- `tests/test_entrypoint_parent_invocation.py`: 5 passed (requires `requests` module; confirmed on both branch + main)
- Total: 18 passed, 0 failed
- Webhook tests: 10 passed (no regression)
- Pre-existing: 8 failures in `test_entrypoint.py` confirmed on main (unrelated, missing deps)

## Security review
- No secrets or credentials in diff
- No new dependencies added
- No network calls or external endpoints introduced
- DDB writes remain fail-soft (no crash path)
- No input validation bypass — `int()` conversion on issue number is fine (comes from env var string already validated upstream)
- No injection risk (boto3 typed attribute format)

## Recommendation
**APPROVE** — all acceptance criteria satisfied, no merge blockers, tests pass, security clean.
