# Review of PR #996

## Driving issue
- #994: fix-from-deploy-test: Phase 4 — codify 3 manual prereqs (OAuth stub, PyJWT layer, namespace mismatch)

## Acceptance criteria checklist
- [x] OAuth stub secret pre-created idempotently before terraform plan — workflow step added between "Install required CLI tools" and "Terraform Init", uses `describe-secret` check + `create-secret` fallback with placeholder JSON
- [x] PyJWT Lambda layer built/uploaded if missing in state bucket — workflow step added, checks `head-object`, builds inline via `build.sh`, uploads to `s3://$STATE_BUCKET/lambda-layers/pyjwt-py313.zip`
- [x] rds-bootstrap namespace default changed from `adp-gateway` → `bedrockgw` — both `variables.tf` default and `main.tf:603` call-site updated
- [x] Stale-job cleanup updated to check both namespaces during migration — loop over `bedrockgw` and `adp-gateway`
- [x] `k8s/namespace.yaml` still says `adp-gateway` (correct — that's for gateway-deploy phase, not rds-bootstrap)
- [x] `cloudwatch_dashboard` module still says `eks_namespace = "adp-gateway"` (correct — monitors running pods, not bootstrap job)
- [x] No `agent_learning/*.md` or forbidden files committed
- [x] Idempotent: re-run shows "already exists / skipping" messages

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `modules/gateway/infra/main.tf:549`: `eks_namespace = "adp-gateway"` in cloudwatch_dashboard is correct for now (monitors pods deployed later), but if the gateway deployment ever migrates pods to `bedrockgw`, this line would need updating. Not actionable today.
- Post-merge cleanup on platform account `<platform-account-id>`: `kubectl delete job -n adp-gateway -l app=bedrockgw-dev-rds-bootstrap` to remove orphaned job from old namespace.

## Security assessment
- No secrets or credentials in the diff (placeholder values are intentional stubs)
- No new dependencies or packages introduced
- No new IAM permissions required (uses existing workflow role)
- `set -e` used in PyJWT build step for fail-fast behavior
- Idempotent checks prevent accidental overwrites of real OAuth credentials

## Recommendation
APPROVE — all three acceptance criteria satisfied, no merge blockers, clean surgical change.
