# Review of PR #1023

## Driving issue
- #1022: fix-from-deploy-test: Phase 8 — codify cross-account inputs + drop hardcoded account_id + runner_image

## Acceptance criteria checklist
- [x] No `var.account_id` declarations remain in `modules/agent-factory/infra/` — grep returns zero matches
- [x] `runner_image` is constructed at terraform plan time from caller identity, not hardcoded — `local.runner_image` uses `data.aws_caller_identity.current.account_id` with fallback to `var.runner_image` override
- [x] Workflow accepts Stage F inputs (account_id, customer_account_id, customer_aws_label, customer_user_id, environment) — matches pattern from gateway-infra-apply.yml
- [x] `Load deployment config` step added after checkout, before CLI tools install — correct position
- [x] Concurrency key includes customer_account_id for parallel cross-account deploys — `tf-apply-agent-factory-${{ inputs.customer_account_id || inputs.account_id || 'default' }}`
- [x] `terraform fmt -check -recursive` passes — confirmed
- [x] `terraform validate` passes — confirmed (pre-existing chat_artifacts lifecycle warning only)
- [x] Each submodule declares its own `data "aws_caller_identity" "current" {}` — confirmed in beads-state, runner-iam, lambda-gateway

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `modules/agent-factory/infra/modules/lambda-gateway/main.tf:38`: Minor whitespace change (`WS_API_ENDPOINT      =` → `WS_API_ENDPOINT =`) — likely auto-formatted by `terraform fmt`. Harmless but noted as it's outside the stated scope (surgical changes principle). Acceptable since fmt normalization is standard practice.
- `terraform.tfvars` still has `github_org = "aws-e"` and `github_app_dev_installation_id = "124731131"` hardcoded — acknowledged in issue body as Piece 4 (out of scope for this PR, documented for future fix).

## Recommendation
APPROVE
