# Review of PR #1185

## Driving issue
- #1153: [Sub of #615] sec/C2: agents run bypassPermissions+Bash on untrusted input — prompt-injection RCE

## PR Summary
Documentation-only PR. Adds `docs/security-c2-prompt-injection-mitigation.md` — the architecture decision document for mitigating prompt injection RCE in agent-factory agents. 137 lines added, 0 deleted. Single file.

## Acceptance criteria checklist

From issue #1153:
- [x] Architect audit landed (trust boundary per call site + persona allowlist proposal) — The detailed audit is in issue comment #7 (18 call sites, trust boundary per each, per-persona shell surface table). The doc captures the summary and actionable plan.
- [x] Chosen mitigation direction documented in this issue — Two-Tier Loop + Prompt Hardening selected as primary. Rationale provided in decision table.
- [x] Sub-tasks filed for each implementation chunk (likely 3-4 PRs) — PR 1-4 decomposition is in the doc (prompt hardening, remove Bash from planners, two-tier refactor, /instruct sanitization). Sub-task issues not yet filed as separate GH issues, but the spec is complete.
- [ ] Red-team injection tests pass — N/A for this PR (architecture doc only, no code changes)
- [ ] Existing agent flows unbroken (48h soak) — N/A for this PR (no behavioral changes)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge

1. **Sub-task issues not yet filed as separate GitHub issues**: The doc defines PRs 1-4 but they haven't been created as trackable issues yet. This is fine for the doc PR itself — filing can happen after merge. Noted for follow-up.

2. **File count discrepancy**: The doc header says "9 files" but lists 10 files in the Scope section (agent-worker, agent-pm, agent-superpower, monitoring, skill-agent, run-query, MCPOnboardPlanningAgent, FixOrchestrator, CodeGenerationAgent, mcp-onboard). The architect's issue comment says "9 files" as well but then lists 10. This is a minor documentation inconsistency (the correct count is 10 files). Not a blocker since the actual per-file detail is correct.

### LOW — follow-up candidates

1. **Line numbers will drift**: The doc references specific line numbers (L589, L793, L1028, L196, etc.) that will shift as other PRs merge. Consider noting "as of commit X" or using function names instead. Not actionable for this PR.

2. **"10 are critical" needs source**: The 10 critical call sites aren't explicitly listed by number in the doc — the reader must cross-reference with the issue comment. A simple numbered list in the Scope section would improve readability.

## Code verification

I independently verified the audit's claims against the actual source code:
- **agent-worker.ts L1103**: Confirmed `bypassPermissions` + Bash + raw issue.body at L662
- **agent-superpower.ts L589/793/1028**: Confirmed all three call sites with `bypassPermissions` + Bash
- **skill-agent.ts L190-196**: Confirmed planning phase has Bash; L443 re-embeds issue.body in execute
- **monitoring.ts L2189/2314**: Confirmed `/queryPM` and `/instruct` pass user text into Bash-enabled prompts

All claims are accurate.

## AGENTS.md compliance
- No `agent_learning/*.md` files committed
- No `environments/dev/backend.tfvars` modifications
- No `.terraform/` files
- No `tfplan` files

## Security review
- No secrets, API keys, tokens, or credentials in the diff
- No executable code changes
- No dependency additions
- Documentation only — zero attack surface change

## Recommendation
**APPROVE** — Documentation-only PR that accurately captures the architectural audit and mitigation plan. All verifiable acceptance criteria are satisfied. The file count discrepancy (9 vs 10) is cosmetic and doesn't affect the plan's correctness.
