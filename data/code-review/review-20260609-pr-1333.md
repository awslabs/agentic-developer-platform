# Review of PR #1333

## Driving issue
- #1332: [Sub of #1287] Consolidate the 4 Personal Context designs into ONE coherent design + ordered roadmap

## PR summary
- Single file added: `docs/design-personal-context-consolidated.md` (723 lines)
- Documentation-only PR — no code changes
- Branch: `agent/issue-1332`

## Acceptance criteria checklist

### Issue requirement 1: "One identity model end-to-end"
- [x] **Satisfied** — Section 2 (Unified Identity Model) covers all 7 entry paths (chat, GitHub human, GitHub bot human-rooted, GitHub bot autonomous, user-facing API, synthesis CronJob, chat hydration hook). All converge to the same `owner_sub` (UUID). Section 2.3 explains WHY they converge (same Cognito `sub` written at onboarding). Section 2.4 defines the fail-closed contract across 6 enforcement layers.
- Cross-checked against actual code:
  - `identity.py:79-81` — UUID validation confirmed
  - `handler.py:635` — bug location confirmed
  - `personal-context-headers.ts:57` — fail-closed confirmed
  - `entrypoint.py:146-148` — env var gating confirmed

### Issue requirement 2: "One isolation invariant, enforced everywhere"
- [x] **Satisfied** — Section 3 defines the single rule (Section 3.1) and maps it to 7 enforcement points (Section 3.2). Section 3.3 explicitly confirms the new #1327 user API uses the SAME logic as `_caller_can_read()`. Section 3.4 confirms no second weaker path exists.
- Cross-checked: `storage.py:214-230` (`_caller_can_read`) matches the pseudocode in the doc.

### Issue requirement 3: "One write model"
- [x] **Satisfied** — Section 4 defines the canonical entry lifecycle (4.1), all 5 writers and their contracts (4.2), the no-double-write guarantee (4.3), the canonical entry schema (4.4), and edit/delete propagation rules (4.5).
- Cross-checked: Entry schema matches `models.py:36-57` exactly (field names, types, defaults).

### Issue requirement 4: "One API surface"
- [x] **Satisfied** — Section 5 defines 3 access patterns (Agent/MCP, User/Gateway API, Job/CronJob+Hooks), all sharing the same store. Section 5.5 enumerates the 5 shared contracts.

### Issue requirement 5: "Consistent risk posture (merged risk register)"
- [x] **Satisfied** — Section 6 contains 24 risks (R1-R24) organized by severity (Critical/High/Medium/Low), with source attribution back to the original design docs. Section 6 "Cross-Cutting Theme" explicitly articulates the defense-in-depth story (7 layers).

### Issue requirement 6: "Sequencing across everything"
- [x] **Satisfied** — Section 8 provides a 7-phase roadmap with explicit dependency graph, GATE marking on Phase 1 (#1319), MILESTONE marking on completion. Appendix B summarizes as a table.

### Issue output 1: "Unified design doc at docs/design-personal-context-consolidated.md"
- [x] **Satisfied** — File exists at the required path.

### Issue output 2: "Contradiction/gap report"
- [x] **Satisfied** — Section 7.1 reports 4 contradictions (all resolved with rationale). Section 7.2 reports 8 gaps (all with required resolution).

### Issue output 3: "One ordered implementation roadmap"
- [x] **Satisfied** — Section 8 with Phases 0-7, ASCII visualization, and Appendix B issue summary table.

### Issue output 4: "Recommendation on whether to supersede per-doc implementation lists"
- [x] **Satisfied** — Section 9 explicitly recommends superseding, with 3 rationale points and the recommended action.

### Non-goals respected: "No implementation code"
- [x] **Satisfied** — PR contains only a markdown design document. No code changes.

### Non-goals respected: "Do not relitigate settled choices"
- [x] **Satisfied** — Decision log (Section 11) records choices as settled; contradictions section reconciles without overturning existing decisions (OpenViking/Neptune store, SA-per-automation, layered raw-vs-distilled all preserved).

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
- **WIP commit.** The branch includes `8296cde WIP: agent/architect starting #1332` as an empty/marker commit. Not a blocker (it's normal for agent workflow), but the squash-merge will flatten this. No action needed.

### LOW — follow-up candidates
- **Line references may drift.** The doc references specific line numbers (`handler.py:635`, `storage.py:122-124`, etc.). These will become stale as code evolves. Acceptable for a point-in-time design doc; consider removing line numbers in future updates.
- **Section numbering gap.** Issue body (CLAUDE.md template) requires section "5" but the doc jumps from §5 to §6. This is about the doc's own numbering — it goes 1-11 + appendices, which is coherent. No issue.
- **Minor: "Section 4" heading in issue asks about decay/supersede lifecycle.** The doc models this (Section 4.1 lifecycle diagram) but doesn't specify exact decay interval in the same detail as the source #1325 doc. The source doc is referenced for implementation detail — acceptable for a consolidation overview.

## Prohibited files check
- [x] No `agent_learning/*.md` files committed
- [x] No `.terraform/` files
- [x] No `tfplan` files
- [x] No secrets/credentials
- [x] Only file in PR: `docs/design-personal-context-consolidated.md`

## Code accuracy verification
Spot-checked the following claims in the consolidated doc against real source code:
- `Persona` enum has exactly 4 values (no `chat`) — ✅ confirmed (models.py:27-33)
- `_caller_can_read()` at storage.py:214 — ✅ confirmed
- Force-stamping at storage.py:122-124 — ✅ confirmed (actually 123-124)
- UUID validation at identity.py:79-81 — ✅ confirmed (80-81)
- `handler.py:635` sets `cognito_sub = resolved.user_id` — ✅ confirmed
- `entrypoint.py:146-148` gates `ADP_OWNER_SUB` on truthy — ✅ confirmed
- `ResolveUserResponse` lacks `cognito_sub` field — ✅ confirmed (routes.py:91-96)
- `personal-context-headers.ts` returns null if ownerSub empty — ✅ confirmed (lines 57, 73)

## Security review
- Documentation-only change — no code execution paths modified
- No secrets, credentials, or sensitive data in the document
- The design doc references proper security patterns (HMAC verification, fail-closed, force-stamping, NetworkPolicy)
- Risk register (Section 6) demonstrates comprehensive threat modeling

## Recommendation
**APPROVE** — All acceptance criteria satisfied. Documentation accurately reflects current codebase state. No prohibited files. No security concerns. No merge blockers.
