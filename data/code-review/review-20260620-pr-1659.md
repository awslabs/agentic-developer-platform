# Review of PR #1659

## Driving issue
- #1658: [Agent Activity] Default-hide no_op / webhook_received from the board (show actual runs, with a 'Show all events' toggle)

## Acceptance criteria checklist
- [x] **Default excludes no_op and webhook_received** — `service.py:215-221`: `elif not include_non_triggering:` adds `~Attr("status").is_in(["no_op", "webhook_received"])` as FilterExpression
- [x] **`include_non_triggering=true` shows all** — when True, the `elif` branch is skipped, no exclusion filter added
- [x] **Explicit status filter takes precedence** — `service.py:213`: `if status:` branch fires first (eq filter), `elif` exclusion is skipped entirely
- [x] **`rejected`/`rate_limited` shown by default** — only `no_op` and `webhook_received` are in the exclusion list; tests explicitly verify rejected/rate_limited pass through
- [x] **Pagination with exclusion filter works** — test `test_pagination_with_exclusion_filter` verifies short/empty pages with non-null `last_key` still returned; filtered-pagination contract preserved
- [x] **Frontend "Show all events" toggle** — `AgentActivity.tsx:449-463`: checkbox wired to `showAllEvents` state, resets pagination on change
- [x] **Frontend defaults to runs-only** — `showAllEvents` initialized to `false`; when falsy with no status filter, `include_non_triggering` is `undefined` (skipped by `buildQueryString`), so backend defaults to `False`
- [x] **Explicit status dropdown works alongside toggle** — `AgentActivity.tsx:236`: `(statusFilter || showAllEvents) ? true : undefined` — selecting any status sends `include_non_triggering=true`
- [x] **Both `/me` and `/admin` endpoints updated** — `routes.py` adds `include_non_triggering` param to both endpoints; tests cover both
- [x] **No data/capture changes** — only display filtering; no schema or write-path changes

## "Must not commit" check
- No `agent_learning/*.md` files in diff ✅
- No `.terraform/` files ✅
- No `tfplan` files ✅
- No credentials/secrets ✅

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `AgentActivity.tsx:460`: Uses `&amp;` HTML entity in JSX text content — renders correctly as `&` in React but some projects prefer the literal character or `{'&'}` for clarity. Non-functional, cosmetic only.

## Verification results
- `ruff check src/activity/ tests/activity/` → ✅ All checks passed
- `ruff format --check src/activity/ tests/activity/` → ✅ 10 files already formatted
- `pytest tests/activity/ -q` → ✅ 99 passed
- `tsc --noEmit` → ✅ Zero errors
- `tsc && vite build` → ✅ Built cleanly in 10.09s
- `vitest run src/__tests__/services/` → ✅ 186 tests passed (9 files)

## Recommendation
**APPROVE** — all acceptance criteria satisfied, no merge blockers, all checks pass.
