# Review of PR #1467

## Driving issue
- #1460: [Provenance] Capture run-to-run parent edge for agent-to-agent lineage

## Acceptance criteria checklist
- [x] **Migration: nullable `parent_invocation_id VARCHAR(255)` column on `action_provenance`** — `017_parent_invocation_id.py` adds nullable column with no backfill. Reversible via `downgrade()`.
- [x] **Model update** — `provenance.py` adds `parent_invocation_id: Mapped[str | None] = mapped_column(String(255), nullable=True)`.
- [x] **API: `parent_invocation_id` accepted in `CreateProvenanceRequest`** — field added with `str | None = None` default.
- [x] **Self-reference guard: `parent_invocation_id == row_id` rejected** — 400 returned with `self_referential_parent` error.
- [x] **Lambda correlation_store: `write_pointer` carries `triggering_invocation_id`** — optional param, conditionally included in DDB item.
- [x] **Lambda correlation_store: `read_pointer` returns `triggering_invocation_id`** — added `item.get("triggering_invocation_id")` to return dict.
- [x] **Lambda gateway_client: `post_provenance` passes `parent_invocation_id`** — new param with `None` default, included in JSON body.
- [x] **Lambda handler: `determine_correlation` returns `parent_invocation_id`** — human=None, bot+pointer=pointer value, bot+no pointer=None.
- [x] **Lambda handler: provenance POST includes `parent_invocation_id`** — line 617: `parent_invocation_id=correlation_ctx.get("parent_invocation_id")`.
- [x] **Lambda handler: envelope carries `parent_invocation_id`** — added to `correlation` dict in envelope (line 673).
- [x] **Worker entrypoint: exports `ADP_MESSAGE_ID`** — line 144: `os.environ["ADP_MESSAGE_ID"] = message_id`.
- [x] **Worker `_write_outbound_correlation`: passes `own_message_id` as `triggering_invocation_id`** — line 906: `triggering_invocation_id=own_message_id or None`.
- [x] **Worker correlation_store: `write_pointer` accepts `triggering_invocation_id`** — conditionally adds to DDB item.
- [x] **Worker provenance_client: `post_provenance` accepts `parent_invocation_id`** — added to body dict.
- [x] **All writes fail-soft** — all paths wrapped in try/except or use existing fail-soft patterns.
- [x] **No backfill** — nullable column, existing rows unaffected.
- [x] **Unit tests: 27 new tests** — 5 gateway, 14 lambda, 8 worker. All passing.
- [x] **Existing test regression** — `test_correlation_store.py` updated to expect `triggering_invocation_id: None` in read_pointer result. All existing suites pass.

## Findings (by confidence)

### HIGH -- merge blockers
None.

### MEDIUM -- fixed during review
- `handler.py:673`: Line exceeded 100-char `line-length` limit (109 chars). **Fixed** — reformatted to multi-line ternary.
- `test_entrypoint_parent_invocation.py`: `@patch.dict` decorator arguments not formatted per ruff rules. **Fixed** — reformatted all three instances.
- `test_correlation_store_parent.py:131-133`: Positional args on single line too long. **Fixed** — split across lines.

### LOW -- follow-up candidates
- The existing `entrypoint.py` has a pre-existing ruff format violation at line ~451 (dict comprehension). Not introduced by this PR; not addressed here.
- The worker `_write_outbound_correlation` does not yet pass `parent_invocation_id` to its `post_provenance` call (line 914). This is consistent with the issue design which says the worker's outbound provenance records the *action it performed*, not the parent. The parent edge is only set on the *inbound* provenance by the webhook handler. No issue.

## Recommendation
**APPROVE** — all acceptance criteria met, tests comprehensive, fail-soft patterns preserved, formatting issues fixed. Pushing fix commit to the PR branch.
