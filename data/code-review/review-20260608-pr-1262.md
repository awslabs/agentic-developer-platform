# Review of PR #1262

## Driving issue
- #1261: gbrain Dockerfile: public.ecr.aws/oven/bun does not exist (#1248 broke the build) — base on public-ECR node + install Bun

## Acceptance criteria checklist
- [x] Both stages use `public.ecr.aws/docker/library/node:22-slim` — lines 2 and 12 of new Dockerfile
- [x] Bun installed via `https://bun.sh/install` in BOTH stages — RUN commands in both build (line 5-7) and runtime (line 18-20) stages
- [x] `ENV BUN_INSTALL=/root/.bun` and `ENV PATH=/root/.bun/bin:$PATH` set in both stages — lines 3-4 and 13-14
- [x] gbrain version pin `@v0.42.26.0` preserved — line 9: `bun install -g github:garrytan/gbrain@v0.42.26.0`
- [x] Existing COPY paths unchanged — `/root/.bun/install/global/node_modules` → `/app/node_modules` and `/root/.bun/bin/gbrain` → `/usr/local/bin/gbrain` (lines 22-23)
- [x] Config COPY preserved — `COPY config/ /app/config/` (line 26)
- [x] HEALTHCHECK preserved — `--interval=30s --timeout=5s --start-period=60s --retries=3 CMD curl -f http://localhost:3000/health || exit 1` (lines 30-31)
- [x] CMD preserved — `["gbrain", "serve", "--http", "--port", "3000"]` (line 33)
- [x] No Docker Hub references — all FROM lines use `public.ecr.aws/docker/library/`; no `oven/bun`, no unprefixed `node:`
- [x] `curl` available in runtime for healthcheck — installed as part of Bun prerequisites (`curl unzip ca-certificates`)
- [x] Only one file modified — `modules/research/gbrain/docker/Dockerfile`

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- Both stages install identical packages (`curl unzip ca-certificates`) and run the Bun installer. This means the runtime image includes `unzip` and `ca-certificates` which are only strictly needed for the install step. The image size impact is minimal (these are small packages on slim), and removing them would add complexity (separate layer or multi-step) for no real gain. Acceptable as-is.
- Consider pinning Bun to a specific version (e.g., `curl -fsSL https://bun.sh/install | bash -s "bun-v1.x.y"`) for fully reproducible builds. Filed as non-goal in the issue.

## Security review
- `curl ... | bash` pattern for Bun installer: standard practice, HTTPS endpoint, official source. LOW risk.
- No hardcoded secrets or credentials.
- No new permissions or attack surface.
- No vulnerable dependencies introduced (node:22-slim is a well-maintained official image).
- No insecure configurations.

## Recommendation
APPROVE — all acceptance criteria satisfied, no merge blockers, no security concerns.
