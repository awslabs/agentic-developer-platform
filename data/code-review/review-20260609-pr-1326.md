# Review of PR #1326

## Driving issue
- #1319: [Sub of #1287] Design: resolve GitHub sender → cognito_sub for Personal Context (+ risk register)
- Specifically: maintainer follow-up comment requesting service-account / autonomous-run extension

## PR Summary
- **Files:** 1 file added (`docs/design-1319-service-account-personal-context.md`, +528 lines)
- **Type:** Design-only (no code changes)
- **Branch:** `agent/issue-1319`

## Acceptance Criteria Checklist

The maintainer's follow-up (comment 13) requested 6 specific design items:

### 1. Owner identity for autonomous runs
- [x] **Satisfied** — Section 3.1 recommends `ServiceAccount.id` (UUID) as `owner_sub`. Justified against 3 alternatives (synthetic string, User.id of bot-kind, fake Cognito sub). Correctly identifies that MCP server validates UUID format only (verified against `identity.py:79`).

### 2. Two-level resolution for event-driven invocations
- [x] **Satisfied** — Section 4:
  - Level 1 (Tenant): Resolved via `installation_id` for GitHub events; event payload `detail.tenant_id` for future EventBridge. Correctly identifies unresolvable-tenant as a hard blocker.
  - Level 2 (Automation-kind): `bot_kind → service_account.name` mapping within the tenant. Resolution chain clearly specified (Steps 1-6 in Section 4.2).

### 3. Reconciliation with chained-run case
- [x] **Satisfied** — Section 5 defines clear precedence table:
  - Human-rooted chains → human's cognito_sub (human always wins)
  - Truly autonomous → ServiceAccount.id
  - Edge case (human sets up monitor, runs daily) correctly handled: setup = human, daily = autonomous

### 4. Lifecycle (provisioning model)
- [x] **Satisfied** — Section 6:
  - Pre-provisioned only (NOT on-demand) — rationale given (sprawl risk, no admin control, naming inconsistency)
  - Provisioning flow specified (admin API `POST /admin/organizations/{org_id}/service-accounts`)
  - Explicitly states SA does NOT need Cognito sub (justified)
  - `name` must match `bot_kind` slug

### 5. Extended risk register for non-human cases
- [x] **Satisfied** — Section 10 has 8 new risks (R11-R18):
  - R11: Cross-tenant attribution (Critical) ✓
  - R12: Unbounded service-identity creation / store sprawl (High) ✓
  - R13: SA store readable by humans (Medium) ✓
  - R14: SA impersonation (High) ✓
  - R15: Stale SA after deletion (Low) ✓
  - R16: bot_kind → SA name mismatch (Medium) ✓
  - R17: Shared IAM role across SAs (Low) ✓
  - R18: Stale correlation pointer claiming human-rooted (Medium) ✓
  - All have severity + likelihood + mitigation

### 6. Updated implementation-issue list
- [x] **Satisfied** — Section 12:
  - Issue D (NEW) clearly scoped: migration, endpoint, gateway_client method, handler logic, envelope field, seed SAs
  - Issues A/B/C unchanged from parent
  - Deployment sequence in Section 13 (5 phases)
  - Issue D explicitly ships AFTER Issue A

## Consistency with Parent Design

- [x] Resolution hierarchy (Section 11) correctly extends parent's 3-case to 4-case, inserting SA case between human-rooted-chain and fail-closed
- [x] Fail-closed invariant preserved (case 4)
- [x] Envelope field reuse (`cognito_sub`) justified — avoids breaking downstream
- [x] Feature flag reuse (`RESOLVE_CANONICAL_VIA_GATEWAY`) is consistent
- [x] Gateway-first deployment order maintained
- [x] `owner_kind` field addition is backward-compatible (defaults to `"human"` if absent)

## Source Code Reference Verification

- [x] `ServiceAccount` model at `organization.py:98` — verified exists with UUID id, TenantMixin (org_id), name, iam_role_arn
- [x] `CallerIdentity` at `identity.py:31-34` — verified UUID format validation only
- [x] `determine_correlation()` at `handler.py:273-312` — verified `is_human_rooted` logic
- [x] `ResolvedIdentity` at `identity_resolver.py:39` — verified `user_kind` field
- [x] `entrypoint.py:142-151` — verified `ADP_OWNER_SUB` from envelope
- [x] `personal-context-headers.ts:47-62` — verified null return if ownerSub empty

## Findings (by confidence)

### HIGH — merge blockers
None identified.

### MEDIUM — discuss before merge
1. **Section 4.2 Step 3: New endpoint naming** — Document calls it `POST /internal/v1/resolve-service-account-owner` in the resolution chain but `POST /internal/v1/resolve-service-account` in Section 8 and Section 12. Minor naming inconsistency — should pick one. Low risk since this is a design doc and implementation will need to choose.

2. **Migration numbering** — Doc assumes `017_service_account_unique_name.py` but this could conflict if other migrations land first. Acceptable for a design doc (implementation will use actual next number).

### LOW — follow-up candidates
1. **Open Questions (Section 14)** — 4 open questions documented (SA admin visibility, shared SAs, retention, unbounded growth). All are explicitly deferred — acceptable for a design doc.

2. **UUID collision between human and SA stores** — Section 7.2 notes "collision probability 2^-122 — structurally infeasible" between Cognito-generated and SQLAlchemy-generated v4 UUIDs. While technically correct (both are random v4), worth a brief callout that both use the same UUID version. Not a real risk but could benefit from a deterministic guard (e.g., `owner_kind` prefix in the storage key).

## Must-Not-Commit Violations
- [x] No `agent_learning/*.md` files in diff
- [x] No `tfplan` files
- [x] No `.terraform/` files
- [x] No secrets, credentials, or API keys
- [x] Single design document only — appropriate for the issue scope

## Recommendation
**APPROVE** — All 6 acceptance criteria from the maintainer's follow-up comment are satisfied with clear justification. The design is consistent with the parent doc, fail-closed invariant is preserved, all code references are verified accurate, and the risk register is comprehensive (8 new risks with mitigations). The two MEDIUM findings are naming inconsistencies within the design doc that don't affect correctness — they can be resolved at implementation time.
