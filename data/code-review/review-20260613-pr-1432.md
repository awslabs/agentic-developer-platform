# Review of PR #1432

## Driving issue
- #1424: [Sub of #1345] Admin UI: Knowledge-layer indexing status page (per-repo, per-stage)

## Acceptance criteria checklist
- [x] Backend APIRouter defined in agent-context (`agent_context.api.indexing_router`) — `GET /api/admin/indexing/runs` + `GET /api/admin/indexing/runs/:run_id` — present at `modules/agent-context/agent_context/api/indexing_router.py`
- [x] Router queries `index_run_stages` + `index_runs` tables — SQL queries at lines 76-108 and 144-192
- [x] Gateway mounts router via conditional `include_router` behind `AGENT_CONTEXT_ENABLED` — `modules/gateway/src/app.py` lines 207-227
- [x] Feature-flagged: only mounted when `AGENT_CONTEXT_ENABLED=true` + ImportError catch for graceful fallback — lines 207, 223-226
- [x] Admin-only access control — **FIXED**: changed from `get_current_user` to `require_admin` (commit 354e402)
- [x] Frontend page at `pages/admin/IndexingStatus.tsx` — 423 lines with two-level drill-down
- [x] Two-level drill-down: runs table (Level 1) + per-repo stage chips (Level 2) — RunRow and RepoStageRow components
- [x] StatCards: Total Repos, Fully Verified %, Failed Stages, Drift Detected — lines 1033-1041 (IndexingStatus.tsx)
- [x] 6 canonical stage chips (clone, cgc_structural, embed_vectors, sbom_source, deepwiki, zoekt_index) with green/red/grey — STAGES constant + statusColor function
- [x] Service methods in `services/admin.ts` — `getIndexingRuns()` + `getIndexingRunDetail()`
- [x] Mock handlers in `mocks/handlers/indexing.ts` — 129 lines of realistic mock data
- [x] Component tests — 10 tests, all passing
- [x] Nav entry for platform admins — Navigation.tsx, guarded by `isPlatformAdmin()`
- [x] Route registered in App.tsx — `/admin/indexing`
- [x] Read-only (no write endpoints) — confirmed, only GET routes
- [x] Drift flag (verified but artifact_ref NULL) — `_count_drift` helper in indexing_router.py

## Findings (by confidence)

### HIGH — merge blockers (resolved)
- `modules/gateway/src/app.py:220`: Used `Depends(get_current_user)` instead of `Depends(require_admin)` — any authenticated user could access admin indexing data. **FIXED** in commit 354e402.

### MEDIUM — discuss before merge
- None.

### LOW — follow-up candidates
- The router uses raw SQL via `text()` rather than ORM models — acceptable for a cross-schema read-only view, but if the schema evolves, these queries need manual updates.
- The Pydantic `IndexRunSummary` model includes `repo_id` which seems to be a per-run artifact from `index_runs.repo_id`, not a per-run aggregate. The actual multi-repo stats come from `_get_run_stats`. The naming might confuse future developers — consider a comment clarifying.
- No pagination controls in the frontend (shows first 20 runs) — acceptable for v1 but consider adding if run counts grow.

## Recommendation
APPROVE — after the `require_admin` fix (already pushed), all acceptance criteria are satisfied and no blockers remain.
