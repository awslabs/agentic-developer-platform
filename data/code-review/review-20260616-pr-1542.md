# Review of PR #1542

## Driving issue
- #1541: [Neptune EPIC #1529] SPIKE-3 (E2E): cgc+SCIP→FalkorDB→CSV→Neptune Bulk Loader→query-back, count-conservation gates 0-4

## Acceptance criteria checklist
- [x] Reusable extractor script (FalkorDB → CSV) — `modules/agent-context/pipeline/neptune_ingestion/extract_falkordb_to_csv.py` (266 lines, well-documented)
- [x] Reusable loader script (CSV → Neptune via openCypher) — `modules/agent-context/pipeline/neptune_ingestion/load_csv_to_neptune.py` (214 lines, SigV4 auth)
- [x] Count conservation verified — 521 nodes exact, 596 edges (611 − 15 dangling = 596) exact
- [x] Gate-by-gate reporting in issue comments (0–4) — all gates posted with numbers
- [x] Findings for #1530 (schema/ID key), #1531 (loader IAM), #1536 (cross-repo/symbol_id) documented
- [x] Cleanup completed (S3 scratch + Neptune data deleted)
- [x] Learnings documented — `agent_learning/2026-06-15-issue-1541-learnings.md`

## Findings (by confidence)

### HIGH — merge blockers
- `agent_learning/2026-06-15-issue-1541-learnings.md`: This file is explicitly excluded by `.gitignore` (`agent_learning/` directory). It was force-added to the commit, violating the repo's "do not commit" rule. Must be removed from the PR before merge.

### MEDIUM — discuss before merge
- None

### LOW — follow-up candidates
- `extract_falkordb_to_csv.py:54`: `line_number` parameter of `make_vertex_id()` is declared but unused within the function body — the disambiguation logic using `line` is in `process_node()` instead. Not a bug (signature documents intent for future use), but could be cleaned up.
- `extract_falkordb_to_csv.py:211-213`: `vertex_ids`, `unique_set`, and `vertex_id_set` — `unique_set` and `vertex_id_set` are both `set(vertex_ids)` computed separately. Could be a single variable. Minor clarity issue only.
- `load_csv_to_neptune.py:40-41`: `Session()` created per call to `neptune_query()` — acceptable for a spike script but would be a bottleneck in production. The function docstring could note this.

## Code Quality
- ✅ `ruff check` passes — zero lint issues
- ✅ `ruff format --check` passes — all files properly formatted
- ✅ Proper CLI interfaces with argparse
- ✅ Good error handling (timeouts, JSON parse errors, Neptune errors)
- ✅ CSV writing uses `csv.DictWriter` (handles quoting properly)
- ✅ Neptune SigV4 auth uses botocore's standard credential chain

## Security Assessment
- ✅ No hardcoded secrets, credentials, or API keys
- ✅ Neptune endpoint passed as CLI argument, not hardcoded
- ✅ subprocess.run uses timeout=120 (no hanging)
- ✅ urllib.request uses timeout=120
- ✅ No shell injection vectors (subprocess uses list, not shell=True)
- ✅ No dangerous operations (read-only from FalkorDB, write-only to Neptune)

## Recommendation
REQUEST CHANGES — remove `agent_learning/` file from the PR (it's gitignored). After that fix, APPROVE.
