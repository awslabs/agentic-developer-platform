# Review of PR #1415

## Driving issue
- #1406: [Sub of #1345] Deploy agent-context to dev (staged) + index eval corpus + run eval

## Context
PR #1415 was admin-merged at 2026-06-12T23:06:14Z (deploy-blocking hotfix pattern).
This is a post-merge review for documentation and correctness verification.
The eval harness was returning 0/15 repos indexed due to Zoekt v16 API format change.

## Change summary
- **1 file modified**: `modules/agent-context/tests/eval/run_eval.py` (+18/-6 lines)
- **1 file added**: `data/code-review/review-20260612-pr-1412.md` (+44 lines, previous review log)

The fix adds dual-key lookup for Zoekt search results: checks `FileMatches` (legacy) then falls back to `Files` (v16+), and adds `FileCount` as a secondary signal for repo-only queries.

## Acceptance criteria checklist

From Step 6 in the deploy comments:
- [x] Eval harness runs successfully against Zoekt v16 — the `FileMatches`/`Files` dual-key pattern is applied in all 3 query functions
- [x] All 15 repos detected as indexed — `FileCount` fallback ensures repo-only queries without content matches still detect indexed repos
- [x] Full eval report generated — Comment 20 shows 50.9% pass rate (28/55 scoreable)
- [x] Pattern is consistent with `door/search_backend.py:167` — same `result_obj.get("FileMatches") or result_obj.get("Files") or []` pattern
- [x] No secrets, no credentials in the diff
- [x] No files outside expected scope (eval harness + review log only)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The unit tests in `tests/unit/test_zoekt_backend.py` still only test `FileMatches` key (no test for `Files` key). A follow-up could add a `test_zoekt_v16_files_key` case to `_parse_zoekt_response` tests, but this is in `door/search_backend.py`'s test file (which already has the v16 pattern), not the eval harness which has no dedicated unit tests.
- The `check_ingestion` function's query changed from `r:{repo.name} f:.` to `r:{repo.name}` — this is a semantic difference (file-content filter removed). For the eval's purpose (repo presence check) this is fine, but worth noting.
- The `review-20260612-pr-1412.md` file was bundled into this PR instead of having its own commit on the branch. Minor — acceptable for deploy-speed hotfix workflow.

## Security review
- No secrets or credentials in the diff
- No shell execution, subprocess calls, or eval/exec
- No dependency additions
- No configuration changes affecting access control
- httpx calls use the same patterns as before (no new endpoints, no auth changes)
- `open()` calls are pre-existing (reading local YAML corpus/golden files), not modified by this PR
- No OWASP Top 10 concerns

## Code quality
- `ruff check`: All checks passed
- `ruff format --check`: 1 file already formatted
- Pattern consistency: dual-key lookup matches the established pattern in `door/search_backend.py:167`
- DRY: extracted `result_data` variable avoids repeated `data.get("Result", {})` calls
- Defensive coding: `or []` fallback at every stage prevents NoneType iteration errors

## Recommendation
**APPROVE** (post-merge confirmation) — the change is correct, minimal, consistent with the codebase pattern, and satisfies all acceptance criteria.
