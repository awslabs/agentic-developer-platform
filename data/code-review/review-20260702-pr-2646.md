# Review of PR #2646

## Driving issue
- #2642: fix(webhook-ingress): add K8s pre-cleanup before terraform destroy

## Acceptance criteria checklist
- [x] Add "Configure kubectl" step — present at line 66-72
- [x] Add "Delete K8s webhook-ingress resources" step — present at line 74-91
- [x] Steps are BEFORE `terraform destroy` — placed after CLI tools install, before Lambda S3 cleanup → before TF destroy
- [x] Delete ScaledJob by name — `kubectl delete scaledjob agent-scaledjob -n adp-agents`
- [x] Delete TriggerAuthentication by name — `kubectl delete triggerauthentication agent-scaledjob-aws-auth -n adp-agents`
- [x] Delete warm-pool Deployment by name — `kubectl delete deployment agent-warm-pool -n adp-agents`
- [x] Delete PriorityClass — `kubectl delete priorityclass adp-agent-overprovision`
- [x] Delete image-prepull DaemonSet by name — `kubectl delete daemonset agent-image-prepull -n adp-agents`
- [x] Delete service account by name — `kubectl delete serviceaccount agent-scaledjob-sa -n adp-agents`
- [x] Delete network policies by name — both `default-deny-egress` and `agent-scaledjob-egress`
- [x] All commands use `2>/dev/null || true` for graceful handling
- [x] `kubectl wait --for=delete pods --timeout=60s` ensures running agent pods terminate
- [x] Resources deleted by specific name (NOT `--all`)
- [x] Namespace NOT deleted (TF-managed, shared with OTEL collector)
- [x] Follows same pattern as `agent-factory-infra-destroy.yml`

## Invariant checks
- [x] adp-agents namespace NOT deleted — confirmed no namespace deletion in diff
- [x] OTEL collector resources NOT touched — no adot-collector, adot-collector-config, adot-collector-sa in delete commands
- [x] No committed files that should not exist (agent_learning/*.md, tfplan, .terraform/) — only modified workflow file

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `Configure kubectl` step uses `set -e` but the `aws eks update-kubeconfig` failure is caught by `|| { echo "WARNING..." }` which means the step succeeds even if the cluster doesn't exist. This is intentional and correct — the subsequent kubectl commands all use `|| true` so they gracefully no-op.
- The KEDA helm release resources (the `keda` namespace) are not cleaned up here. This is correct — KEDA is managed by Terraform's `helm_release.keda` resource which will handle its own cleanup during `terraform destroy`. Pre-deleting the ScaledJob and TriggerAuthentication CRs unblocks the KEDA operator cleanup.

## Recommendation
APPROVE — all acceptance criteria satisfied, no invariant violations, clean surgical change.
