# Review of PR #1614

## Driving issue
- #1612: [KL-agent #1586] bug(context-mcp): /mcp returns 500 — MCP session-manager lifespan not composed (Task group not initialized)

## Acceptance criteria checklist
- [x] **Compose `mcp_server.session_manager.run()` into parent app's `lifespan()`** — Done at `server.py:185` (`async with mcp_server.session_manager.run():`)
- [x] **Backend init BEFORE session_manager.run()** — Lines 138-175 initialize all backends (Zoekt, S3, DB, Neptune, ACL, experience tool) before line 185 starts the session manager
- [x] **Use correct accessor for mcp 1.28.x** — Verified: `mcp_server.session_manager.run()` returns `AsyncIterator[None]` on mcp 1.28.0
- [x] **`stateless_http=True` preserved (architect C2)** — Confirmed in `mcp_app.py:135`, unchanged by this PR
- [x] **Integration test with real lifespan** — `test_mcp_lifespan.py` uses `LifespanManager` (not bare `ASGITransport`) — 6 tests cover initialize, tools/list, ACL fail-closed, and REST regression
- [x] **ACL parity under live lifespan** — `TestACLUnderLiveLifespan::test_mcp_search_no_identity_returns_empty` exercises fail-closed with real session manager running
- [x] **Legacy REST unaffected** — 3 regression tests verify `/health`, `GET /tools`, `POST /call` still return 200 under live lifespan
- [x] **No double-start / resource leak** — `session_manager.run()` is called once in lifespan, properly scoped in async-with (teardown on shutdown)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- `pyproject.toml:13`: `asgi-lifespan>=2.1` added to main `dependencies` rather than `[project.optional-dependencies]` (test-only dep). Functionally harmless since this `pyproject.toml` is only used for the test suite (`name = "agent-context-tests"`) — not the production Docker image. No action needed.
- `server.py:183`: The `# noqa: E402` comment is technically unnecessary since ruff doesn't flag function-body imports with E402 (it's a module-level rule). Cosmetic only — not a merge concern.

## Recommendation
**APPROVE** — The fix is minimal, correct, well-ordered, and closes the exact test gap that allowed the bug to ship. All 34 MCP tests pass (28 existing + 6 new). No regressions to REST endpoints.
