# Review of PR #2575

## Driving issue
- #2575 / #2563: fix(platform-infra): conditional runner-role EKS access entry

## Acceptance criteria checklist
- [x] Use `data "aws_iam_roles"` to check if the runner role exists — satisfied at line 41-44 of PR diff (new data source with `name_regex` + `path_prefix`)
- [x] Conditionally include the runner role ARN only when it exists — satisfied at line 36 of PR diff: `length(data.aws_iam_roles.ci_runner.arns) > 0 ? [local.ci_runner_role_arn] : []`
- [x] First deploy (role absent): admin set = deployer + extras only — verified by logic: empty arns → empty list in concat
- [x] Subsequent deploys (role exists): admin set = deployer + runner + extras — verified: non-empty arns → includes `[local.ci_runner_role_arn]`
- [x] Prevents cascading `for_each` failure on access policy associations — verified: `aws_eks_access_entry.admins` uses `toset(var.cluster_admin_principal_arns)`, removing the invalid ARN from the set means no invalid `for_each` key
- [x] `terraform fmt -check` passes — verified locally ✅
- [x] `terraform validate` passes — verified locally ✅
- [x] No changes to behavior when role exists (backward compatible) — verified: same ARN included, just conditionally

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge

1. **data source attribute type**: `data.aws_iam_roles.ci_runner.arns` is typed as `set(string)` in the AWS provider. Using `length()` on a set is valid in Terraform (converts to list internally for length check), so no issue here. Just noting for clarity.

2. **Regex character escaping in `name_regex`**: The regex `^${local.name_prefix}-agent-runner-role$` contains a literal hyphen `-` which is safe in this position (not inside a character class). No issue, just noting the pattern is correct.

### LOW — follow-up candidates

1. **Comment references #2563 vs #2575**: The data source comment and inline comment both reference "Issue #2563" which is the original bug report. This PR is #2575 (the fix). Both are valid since the PR fixes #2563. No action needed.

2. **`ci_runner_role_arn` local is still defined even when unused**: When the role doesn't exist, the local `ci_runner_role_arn` is computed but never passed to any resource. This is harmless (it's just a string interpolation in a local, not a resource creation), but could be noted for future cleanup. Not worth changing now.

## Security Review

- No credentials, tokens, or secrets in code
- No new IAM permissions granted — the data source `aws_iam_roles` is read-only (uses `iam:ListRoles` which the deployer already has)
- No new network exposure
- No user-controlled input flows into the regex (only `var.name_prefix` / `var.environment` which are operator-controlled tfvars)
- The `path_prefix = "/"` is the broadest search scope but combined with the exact `name_regex` anchor, this is safe

## Terraform-specific checks

- ✅ No provider version change needed (`~> 5.0` covers 5.100.0 which has `aws_iam_roles`)
- ✅ State migration: no state impact — the data source is read-only and doesn't create state entries for existing resources
- ✅ Plan impact on existing deployments: on an existing deployment where the role exists, re-running plan will:
  - Add the new data source read (harmless)
  - No change to `cluster_admin_principal_arns` (role exists, conditional evaluates the same way)
  - Net plan: no resource changes (only the data source refresh)

## Files in diff
- `platform/infra/main.tf` — the only file modified

## Recommendation
**APPROVE** — Clean, minimal fix that solves the first-deploy chicken-and-egg problem without introducing risk to existing deployments. Well-commented, passes validation, no security concerns.
