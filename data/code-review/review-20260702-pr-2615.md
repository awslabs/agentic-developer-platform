# Review of PR #2615

## Driving issue
- #2615 / #2590: fix: remove duplicate EKS→RDS/Redis SG rules from gateway module

## Acceptance criteria checklist
- [x] Removes `aws_security_group_rule.eks_cluster_to_rds` from gateway/infra/main.tf — confirmed removed (lines 643-661 deleted)
- [x] Removes `aws_security_group_rule.eks_cluster_to_redis` from gateway/infra/main.tf — confirmed removed (lines 663-676 deleted)
- [x] Updates `rds_bootstrap` depends_on to remove dangling reference — confirmed (line 706 removed)
- [x] `terraform fmt -check` passes — confirmed (exit code 0)
- [x] No dangling references to removed resources — confirmed (grep shows 0 matches in PR branch)
- [x] Platform infra owns equivalent rules — confirmed (`rds_from_eks_cluster` at platform/infra/main.tf:186 and `redis_from_eks_cluster` at line 196)
- [x] Same security groups referenced — confirmed (both use same SG IDs via platform outputs)

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The comment "Must run AFTER RDS is available and SG rules allow EKS → RDS" on line 686 of the PR branch is still accurate — platform infra Phase 3 creates the SG rules before gateway Phase 4 runs `rds_bootstrap`. No action needed, just noting the implicit cross-phase dependency is documented in code comments.

## Terraform Plan Verification
CI plan output (Comment 1) confirms exactly 2 SG rule resources are destroyed:
- `aws_security_group_rule.eks_cluster_to_rds` (sgrule-3782717861)
- `aws_security_group_rule.eks_cluster_to_redis[0]` (sgrule-3949607722)

Both marked "(because ... is not in configuration)" — expected behavior.

## Recommendation
APPROVE — Clean, surgical removal of duplicate resources. The platform infra equivalents are verified to exist and cover the same security groups/ports. The `depends_on` cleanup removes the dangling reference correctly. Terraform plan confirms expected behavior.
