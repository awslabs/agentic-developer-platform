# Review of PR #2635

## Driving issue
- #2628: deploy-all.sh --destroy: add webhook-ingress module to orchestrated teardown

## Acceptance criteria checklist
- [x] Adds webhook-ingress step between agent-context and agent-factory — Step 2/5 inserted at correct position
- [x] Pre-cleans K8s KEDA ScaledJobs before terraform destroy — `kubectl delete scaledjobs --all -n adp-agents`
- [x] Pre-cleans `adp-agents` namespace — `kubectl delete namespace adp-agents --wait=true --timeout=120s`
- [x] Removes Lambda artifacts from S3 — `aws s3 rm "s3://${STATE_BUCKET}/lambda-artifacts/webhook-ingress/" --recursive`
- [x] Renumbers existing steps — 1/5 Agent Context, 2/5 Webhook Ingress, 3/5 Agent Factory, 4/5 Gateway, 5/5 Platform
- [x] Gateway sub-step comments renumbered — 3a-3g → 4a-4g (second commit `bd3c737`)
- [x] Correct namespace `adp-agents` used — verified from `scaledjob.tf:27` (`name = "adp-agents"`)
- [x] Correct backend config path — `../../../../environments/$ENVIRONMENT/modules/webhook-ingress-backend.tfvars` (verified file exists at `environments/dev/modules/webhook-ingress-backend.tfvars`)
- [x] Correct var-file — `terraform.tfvars` co-located in `modules/agent-factory/webhook-ingress/infra/`
- [x] Non-interactive flags — `terraform init -input=false`, `terraform destroy -auto-approve`
- [x] All commands have error suppression — `2>/dev/null || true` on cleanup steps
- [x] Syntax valid — `bash -n` passes
- [x] grep count = 10 — confirmed
- [x] Existing destroy order for other modules unchanged — only step numbers changed

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The dedicated `webhook-ingress-destroy.yml` workflow does NOT do K8s namespace pre-cleanup (the orchestrator does it better). Consider adding `kubectl delete scaledjobs --all -n adp-agents && kubectl delete namespace adp-agents` to the dedicated workflow as well for consistency. (Not blocking — the workflow runs from an ARC runner that may not have kubectl context set up the same way.)
- The issue #2628 design section originally mentioned namespace `adp-gateway-agents` but the PR correctly uses `adp-agents` (the actual webhook-ingress namespace). The issue design had an error; the implementation is correct.
- CLAUDE.md line 175 ("Runs the per-module destroys in reverse deploy order: agent-context, agent-factory, gateway, platform") should be updated to include webhook-ingress. Non-blocking follow-up.

## Security Review
- No hardcoded secrets, tokens, or credentials in the diff
- No user-controlled input reaches commands — all variables (`$STATE_BUCKET`, `$ENVIRONMENT`, `$ROOT_DIR`) are derived from script-internal configuration
- S3 path is properly double-quoted to prevent word-splitting
- All destructive commands use `|| true` to prevent script abort on partial failure (appropriate for teardown)
- No new dependencies or external URLs introduced
- No OWASP vulnerabilities applicable (infrastructure script, not a web application)

## Recommendation
**APPROVE** — The PR correctly implements the acceptance criteria from #2628. The code follows established patterns from other destroy steps in the same file, uses the correct namespace and paths (verified against the actual terraform source), and introduces no security concerns.
