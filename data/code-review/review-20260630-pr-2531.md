# Review of PR #2531

## Driving issue
- #2529: fix(agent-context): reset stale Neptune driver on connection failure
- Root cause: #2526 — Neptune driver staleness causing silent fallback to code-index after ~1.5h

## Acceptance criteria checklist
- [x] New `reset_neptune_driver()` function — `door/neptune_client.py:57-74`; closes driver with try/except, sets `_driver = None`
- [x] Updated `neptune_available()` resets-and-retries on failure — `door/neptune_client.py:106-121`; catches exception, calls `reset_neptune_driver()`, gets fresh driver, retries once
- [x] Returns `False` only if retry also fails — line 121: second except returns False
- [x] 5 unit tests for staleness recovery — `tests/unit/test_neptune_client.py` lines 396-497, `TestDriverStalenessRecovery` class
- [x] Pre-existing tests remain green — 25 pass, 4 pre-existing failures (missing deps, unrelated)
- [x] ruff check + format pass — verified locally

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `reset_neptune_driver()` function uses a bare `except Exception` on the close path — this is intentionally defensive (per issue spec), but a follow-up could consider logging the specific exception type at debug level for operational observability.
- The retry logic in `neptune_available()` only handles the health-check path. Other query functions (`query_impact`, `query_understand`, etc.) still fail on stale drivers without retry. This is explicitly out of scope per the issue design (option A: triggered exactly when staleness manifests in the health check), but worth noting for future work.

## Security review
- No secrets, credentials, or tokens in code
- No user input processing (internal health check only)
- No injection vectors (hardcoded `RETURN 1` Cypher query)
- No new dependencies introduced
- Error messages don't leak sensitive information

## Recommendation
APPROVE — all acceptance criteria met, no blocking issues, minimal and surgical change.
