# Review of PR #1390

## Driving issue
- #1346: [Sub of #1345] Replace Sourcebot (FSL-1.1-ALv2) code search with a permissively-licensed equivalent

## Status: CLOSED AS DUPLICATE

PR #1390 is a duplicate of the already-merged PR #1389 ("feat(agent-context): Zoekt-direct serving layer (replaces Sourcebot FSL-1.1)"), which was squash-merged to main at 2026-06-12T16:16:54Z.

## Evidence

1. **Both PRs target the same branch name** (`agent/issue-1346`) — the second architect run force-pushed over the first reviewer's merge commit
2. **All 4 new files are byte-for-byte identical** to what's on main from PR #1389:
   - `docs/design-notes/1346-zoekt-direct-replacement.md`
   - `modules/agent-context/door/search_backend.py`
   - `modules/agent-context/manifests/zoekt.yaml`
   - `modules/agent-context/tests/unit/test_zoekt_backend.py`
3. **`config.py` modification already exists on main** — the `zoekt_url` and `zoekt_timeout` fields were merged via PR #1389
4. **The PR branch is stale** — it diverges from main (3 commits behind) because PR #1388 (OpenViking→S3 rewire) merged after the branch was created

## Root cause

Two concurrent architect agent runs for the same issue (#1346):
- Run 1 (comment #1): produced PR #1389, which was reviewed and merged (comments #13-14)
- Run 2 (comment #4): produced PR #1390 with identical content after #1389 was already merged

## Acceptance criteria verification

All acceptance criteria from issue #1346 were satisfied by the **already-merged PR #1389**:
- [x] Zoekt K8s deployment manifest — `modules/agent-context/manifests/zoekt.yaml`
- [x] Zoekt search backend — `modules/agent-context/door/search_backend.py`
- [x] Config integration — `images/ingestion/config.py` (`zoekt_url`, `zoekt_timeout`)
- [x] Design document — `docs/design-notes/1346-zoekt-direct-replacement.md`
- [x] Unit tests — `tests/unit/test_zoekt_backend.py` (17 tests in PR #1389, 24 tests in PR #1390 — functionally equivalent coverage)
- [x] No edits to `sqs-worker.py` / `ingest-repo.py` (deferred per scope)
- [x] No Sourcebot manifest deletion (kept revertable per scope)
- [x] All linting passes (`ruff check`, `ruff format --check`)
- [x] All unit tests pass

## Recommendation

CLOSE — no merge needed. All content is already on main. Closing without merge to avoid confusion.
