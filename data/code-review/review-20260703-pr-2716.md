# Review of PR #2716

**Reviewer:** @agent-reviewer · **Date:** 2026-07-03
**PR:** #2716 — `docs(#2703): spike note — Codex CLI → Bedrock model path (verified GO)`
**Head branch:** `agent/issue-2714` → `main`
**Diff:** +267 / -0, single ADDED file `docs/agent-context/design-notes/2703-codex-bedrock-model-path-spike.md`

## Driving issue
- #2703: Spike — validate Codex CLI → Bedrock model path (model ID, CLI version, auth mode). Sub of EPIC #2702.

## Acceptance criteria checklist (from #2703 "Validation")
- [x] **Reproducible headless `codex exec` transcript against the chosen backend** — satisfied by §5.2 (lines 141–166): `codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -o <file>`, exit 0, captured output, 7,811 tokens, model `openai.gpt-5.5` via `amazon-bedrock`.
- [x] **Recommendation actionable by story 2 (#2704) without further research** — satisfied by §8 handoff (lines 239–255): 6 concrete steps (pin CLI, bake config.toml, invoke flags, leave bearer unset, egress gotcha, smoke-test the one open IAM item). §7.2 scopes the single deferred check to #2704's smoke test.

Content-quality: also answers all four #2703 design questions (model availability & why `list-foundation-models` misses `gpt-5.5`; CLI version + minimal config; headless auth mode; latency). Verdict **GO** is well-supported.

## Findings (by confidence)

### HIGH — merge blockers
- **Duplicate of already-merged work at the same path.** PR **#2714** (state: **MERGED**, +244) already added the identical file `docs/agent-context/design-notes/2703-codex-bedrock-model-path-spike.md` to `main`, and #2714 also resolves #2703 (same EPIC, same correlation id). The file is present on `main` today. Merging #2716 would create an add/add conflict and, if force-resolved, **overwrite the already-merged #2714 note** with a second, differently-structured version of the same spike. `mergeStateStatus` is currently `UNKNOWN`/dirty. Two design notes for one spike at one canonical path is a maintenance hazard (future readers won't know which is authoritative). **Recommendation: do not merge #2716; the #2703 spike is already resolved on main.**

### MEDIUM — discuss before merge
- If reviewers judge #2716's version materially better than the merged #2714 version (it is more structured: explicit Fallback §6, Verified-vs-Deferred §7 table, standalone Recommendation §9), the correct action is a **separate follow-up PR that replaces/supersedes** the merged file with a clear commit message — not this parallel PR. The two versions are consistent on every material fact (CLI `0.142.5`, `openai.gpt-5.5` on mantle `/openai/v1`, IRSA SigV4 zero-secret, `gpt-oss-120b` fallback, one open IAM item for #2704), so there is no correctness gap forcing a swap.

### LOW — follow-up candidates
- The `codex exec` transcript output (§5.2) differs from the merged note's (`is_palindrome` here vs `is_prime` in #2714) — cosmetic, both are plausible illustrative transcripts; no action needed.

## Security review
- Doc-only change; no code, no infra, no dependency changes.
- Scanned the file for credential patterns (AKIA/ASIA/bearer/api-key/PEM): **none**. All "secret"/"token"/"bearer" mentions are architectural ("zero secrets", "`AWS_BEARER_TOKEN_BEDROCK` unset"). No hardcoded credentials, endpoints, or account IDs beyond the non-sensitive public mantle hostname and IAM **role name** (`adp-dev-agent-scaledjob-role`, not an ARN with account id).
- No "must not commit" violations (no `.terraform/`, `tfplan`, `agent_learning/*.md`, or state files in the diff).
- CI security gates green at review time: Checkov, Bandit, detect-secrets, cfn-nag, npm audit all pass; Grype/Syft/Semgrep pending (doc-only — no material risk).

## Recommendation
**REQUEST CHANGES (do not merge) — close as duplicate of merged #2714.**

The content fully meets #2703's acceptance criteria and is high quality, but the spike is **already resolved and merged on `main` via #2714** at this exact path. Merging #2716 duplicates/overwrites that work. Suggested resolution: **close #2716** (spike done). If the team prefers #2716's structure, open a focused follow-up PR that explicitly supersedes the merged note, rather than landing a second parallel copy.
