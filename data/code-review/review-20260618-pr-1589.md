# Review of PR #1589

## Driving issue
- #1586: [Neptune EPIC #1529] Make Knowledge Layer usable by agents — register MCP Door + repo discovery + query guidance

## PR Summary
- Architecture design document: `docs/agent-context/design-1586-knowledge-layer-agent-integration.md` (576 lines)
- Agent learnings file: `agent_learning/2026-06-18-issue-1586-learnings.md` (93 lines)
- This is a **doc-only PR** — no code changes, no runtime impact.

## Acceptance Criteria Checklist

The issue asks for a design addressing:

- [x] **Discovery mechanism** — how agents learn which repos are indexed + per-repo capabilities
  - §4 of the design doc: enriches existing `browse` verb with coverage metadata (zoekt_status, vectors_status, structure_status, sbom_status) from Postgres `repositories` table. No new tool (avoids increasing decision surface). Well-reasoned.

- [x] **Invocation mechanism** — register the Door as MCP server in agent-worker runtime
  - §2 + §5: in-process tool port pattern (AgentTool[]), matching existing vault/memory/artifact pattern. KnowledgeLayerPort with HTTP client calling the Door. 5 tools registered (search, understand, impact, browse, experience). Correct pattern.

- [x] **Identity passthrough** — agent's principal flows to Door's ACL
  - §3: Identifies the critical gap (GitHub login not propagated via entrypoint.py), proposes fix (ADP_GITHUB_LOGIN env var from SQS envelope → X-GitHub-Login header). Covers both webhook and chat-agent paths. Fail-closed by design.

- [x] **Usage guidance** — system-prompt/skill teaching verbs + navigate-then-read workflow
  - §6 + §7: `<knowledge-layer>` system prompt section with explicit when-to-use rules. Separate skill file (`.claude/skills/knowledge-layer/SKILL.md`) for webhook agents. Persona-specific one-liners.

- [x] **Reconciles with #1536** — deferred agent-consumption design
  - §15: explicitly reconciles — verdict-first output, task-shaped verb descriptions, navigate-then-read workflow all addressed.

- [x] **Flags dependency on #1587** — Door prefers S3 fallback over Neptune
  - §16 + §9: prominent dependency flag, feature flag `KNOWLEDGE_LAYER_ENABLED=0` gates activation until #1587 is verified.

- [x] **Security model** — ACL fail-closed, cross-tenant leak prevention
  - §10: security considerations table covering header fabrication, cross-tenant leak, DoS, identity spoofing. All mitigated.

- [x] **Testing strategy** — unit, integration, smoke tests
  - §11: complete testing strategy with specific test cases and verification commands.

- [x] **Implementation phasing** — independently mergeable steps
  - §12: 6 phases across 2 waves, each independently deliverable.

## "Must not commit" Check

- `agent_learning/2026-06-18-issue-1586-learnings.md` — **VIOLATION** (HIGH)
  - `.gitignore` line 62-63 explicitly says: "Agent-generated per-issue learnings dumps — do not commit to repo" and lists `agent_learning/`
  - This file must be removed from the PR before merge.

## Findings (by confidence)

### HIGH — merge blockers

1. **`agent_learning/2026-06-18-issue-1586-learnings.md` must be removed from the PR**
   - `.gitignore` explicitly excludes `agent_learning/` with "do not commit to repo" comment
   - This was force-added (`git add -f`) to bypass the gitignore — repo policy violation
   - Previous PRs (#1542, #1385) were also blocked for this exact same issue
   - **Fix**: Remove this file from the branch before merge

### MEDIUM — discuss before merge

2. **Port discrepancy noted but not definitively resolved in the doc**
   - §3 mentions the `:8080` vs `:5100` discrepancy (from §Important Issue #3 in the architect's review comment) but the design doc doesn't commit to which is canonical
   - This is acceptable for a design doc (flagged for implementation), but the implementer will need to resolve it first
   - **Not a blocker** for the design doc itself

### LOW — follow-up candidates

3. **Design doc references `envelope.py line 79`** — this line number may drift as the file evolves. Could use function names instead for more stable references. Minor.

4. **`remember` verb excluded from tool registration** — doc says it's already handled by experience-save hook. Correct per current codebase, but worth noting this creates a tool asymmetry (5 of 6 Door verbs exposed).

## Security Assessment

- No code changes in this PR — pure documentation
- No secrets, credentials, API keys, or tokens in either file
- No vulnerable dependencies introduced
- No configuration changes
- The design document's §10 (Security Considerations) is thorough and appropriate
- Identity model is fail-closed by design (correct security posture)

## Recommendation

**REQUEST CHANGES** — Remove `agent_learning/2026-06-18-issue-1586-learnings.md` from the PR (it's gitignored per repo policy). After that fix, **APPROVE**.

The design document itself is excellent:
- All 6 claims about current codebase state verified against actual code
- Architecture decisions are well-reasoned with options analysis
- Phasing is realistic and independently deliverable
- Security model is sound (fail-closed identity bridge)
- Dependencies flagged prominently (#1587 blocker)
- Implementation files and modification list is precise
