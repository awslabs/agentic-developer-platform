# Review of PR #1253

## Driving issue
- #1244: [Sub of #1219] Execute gbrain end-to-end deploy — autonomous, self-healing (apply → build → run → DB init → smoke test)

## PR Summary
Updates `smoke-test.sh` and `seed-brain.sh` for gbrain v0.42.26's MCP Streamable HTTP transport (SSE). Also adds `agent_learning/2026-06-07-issue-1244-learnings.md`.

## Acceptance criteria checklist

From issue #1244 Validation section:
- [x] CodeBuild build `SUCCEEDED`; ECR has a recent `latest` image — N/A (deploy, not this PR)
- [x] ECS service `1/1 running`, task `RUNNING`, stable — N/A (deploy, not this PR)
- [x] `vector` extension present; migrations applied; seed completed — N/A (deploy, not this PR)
- [x] `smoke-test.sh` passes all checks (health, tools/list, write, read) — PR fixes the script to work with SSE transport
- [x] Safety regression: `GBRAIN_ENABLED` still absent/false in agent pods — PR does not enable it ✓
- [x] `seed-brain.sh` works with MCP Streamable HTTP transport — PR updates the script

Script correctness:
- [x] POST to `/mcp` path instead of root — both scripts updated ✓
- [x] Include SSE Accept header (`application/json, text/event-stream`) — both scripts updated ✓
- [x] Parse SSE `data:` response lines — both scripts extract via `grep "^data:" | head -1 | sed 's/^data: //'` ✓
- [x] Use `put_page` tool instead of removed `capture` — smoke-test.sh ✓, seed-brain.sh ✓
- [x] `search` tool name kept (unchanged from original) — ✓

## Findings (by confidence)

### HIGH — merge blockers

1. **agent_learning/2026-06-07-issue-1244-learnings.md**: This file is committed to the PR but `AGENTS.md` line 19 explicitly states: "**Never commit `agent_learning/*.md`** — that directory is gitignored; explicit `git add` bypasses the ignore and has caused drift five times already." The `.gitignore` also contains `agent_learning/`. This file must be removed from the PR.

### MEDIUM — discuss before merge

None.

### LOW — follow-up candidates

1. **smoke-test.sh line ~76**: The write-test error detection logic `grep -qi "error\|fail\|unknown"` could produce false positives if the `put_page` tool returns a success message containing the word "unknown" in its text. Existing behavior; not introduced by this PR. Acceptable.

2. **seed-brain.sh fallback**: The `[ -z "$RESULT" ] && RESULT='{"error":{"message":"empty response"}}'` fallback is good defensive coding for SSE parse failures.

## Recommendation

**REQUEST CHANGES** — remove `agent_learning/2026-06-07-issue-1244-learnings.md` from the commit. The script changes (`smoke-test.sh` and `seed-brain.sh`) are correct and well-structured; approve once the forbidden file is removed.
