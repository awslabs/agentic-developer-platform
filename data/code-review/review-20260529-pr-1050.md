# Review of PR #1050

## Driving issue
- #1049: [fix-from-deploy-test] arc-runner-build.yml needs cross-account (Stage F) dispatch inputs

## Acceptance criteria checklist
- [x] `arc-runner-build.yml` accepts cross-account dispatch inputs (same pattern as `agent-factory-infra-apply.yml`) — inputs added at lines 19-39, matching exact field names/descriptions/defaults
- [x] Workflow creates the `adp-arc-runner` ECR repo in the customer account if it doesn't exist — already handled by `codebuild/bs-arc-runner.yml` pre_build phase (line 22-28 of buildspec)
- [x] Workflow builds and pushes the arc-runner image to `<customer_account>.dkr.ecr.us-east-1.amazonaws.com/adp-arc-runner:latest` — fixed in reviewer commit: `EFFECTIVE_ACCOUNT="${CUSTOMER_ACCOUNT_ID:-$ACCOUNT_ID}"` pattern now used for REGISTRY
- [x] Runner pods in customer account can pull the image (no ImagePullBackOff on non-placeholder-secret failures) — image is pushed to correct ECR; pull depends on IRSA/node role which is out of scope for this workflow

## Findings (by confidence)

### HIGH — merge blockers
- (FIXED) `.github/workflows/arc-runner-build.yml:120`: REGISTRY used `${ACCOUNT_ID}` directly instead of `${CUSTOMER_ACCOUNT_ID:-$ACCOUNT_ID}`. This meant even with cross-account inputs provided, the image would push to platform ECR. Fixed by reviewer in commit `bb1d730`.

### MEDIUM — discuss before merge
- None

### LOW — follow-up candidates
- The concurrency group uses `inputs.customer_account_id || inputs.account_id || 'default'` — on push-triggered runs (not workflow_dispatch), `inputs` context is empty so all push builds share the 'default' group. This is fine since push builds always target the platform account, but worth noting.

## Recommendation
APPROVE (after reviewer fix landed)
