# Review of PR #1385

## Driving issue
- #1383: [Sub of #1345] Implement OpenViking → S3 Vectors + S3/Mountpoint (per design-1348)

## Acceptance criteria checklist
- [x] S3AGFSBackend implements put/get/delete/list_prefix protocol — `personal_context/backends/s3_backend.py` (111 lines, clean interface)
- [x] S3VectorsEmbeddingStore with per-user indexes (hard physical isolation) — `personal_context/backends/s3_vectors_backend.py` (179 lines)
- [x] Persistence across pod restart proven by test — `test_s3_vectors_backend.py::TestPersistenceAcrossRestart`
- [x] Terraform: S3 Vectors bucket + N=4 hash-sharded code indexes — `terraform/modules/s3-vectors/main.tf`
- [x] IAM: s3vectors:* scoped to `arn:...:vector-bucket/adp-*` — `terraform/modules/iam/main.tf`
- [x] Feature flag: STORAGE_BACKEND=openviking|s3 (default s3) — `config.env`, `config.py`, `synthesis.py`
- [x] Centralized config (per #1379) — new settings added to `config.py`, NOT scattered `os.getenv`
- [x] Ingestion pipeline updated for S3 writes — `ingest-repo.py`, `publish-ingestion.py`
- [x] Ingestion S3 helper modules — `s3_content.py`, `s3_vectors_ingest.py`
- [x] OpenViking manifests removed (7 files deleted, ~720 lines) — confirmed
- [x] Existing isolation tests pass unchanged — 34 pass (verified locally)
- [x] Existing experience_tool tests pass unchanged — 23 pass (verified locally)
- [x] New unit tests pass — 17 pass (S3AGFSBackend + S3VectorsEmbeddingStore)
- [x] Total 141 tests pass with zero regressions
- [x] Tenant isolation preserved — `_caller_can_read` in `storage.py` untouched, per-user index isolation added
- [x] Dual-write transition: OpenViking backend retained behind feature flag
- [x] ruff check + ruff format pass on all new/modified Python files
- [x] terraform fmt -check passes on new Terraform module
- [x] Shard routing: SHA-256 of org_id modulo shard_count (per design §8)
- [x] Scatter-gather query across all shards (per design §8)
- [x] Batch writes (500 per call, exponential backoff on throttling)
- [x] Lazy index creation (ensure_index on first save, ConflictException handled)

## Findings (by confidence)

### HIGH — merge blockers
- `agent_learning/2026-06-12-issue-1383-learnings.md` is committed but `agent_learning/` is in `.gitignore` with explicit comment: "Agent-generated per-issue learnings dumps — do not commit to repo". This file must be removed from the PR.

### MEDIUM — discuss before merge
- (none)

### LOW — follow-up candidates
- `ingest-repo.py` line 158: `from s3_content import upload_content` uses a bare import (no package prefix). Works because the ingestion image adds the directory to PYTHONPATH, but could be fragile if the module structure changes. Low priority since it's consistent with existing patterns in the codebase.
- Design note mentions Mountpoint CSI PV/PVC (§5.2) — not implemented in this PR, which is fine (noted as follow-up per #1354).
- Pre-existing lint issues in `ingest-repo.py` and `publish-ingestion.py` (F401, E402, F841) are documented in PR description — not introduced by this PR.
- The `deploy.sh` was edited during development (per progress log) but those changes were excluded from the final diff — good surgical discipline.

## Recommendation
REQUEST CHANGES — remove `agent_learning/2026-06-12-issue-1383-learnings.md` from the branch, then APPROVE.
