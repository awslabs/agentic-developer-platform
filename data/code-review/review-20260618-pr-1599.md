# Review of PR #1599

## Driving issue
- #1591: [KL-agent #1586] Propagate github_login identity to the Door (X-GitHub-Login) — unblocks ACL

## Acceptance criteria checklist
- [x] Export `actor.github_login` from SQS envelope as `ADP_GITHUB_LOGIN` env var — `entrypoint.py:163-165` does exactly this
- [x] ADDITIVE only — Cognito identity rail (`ADP_OWNER_SUB`, `ADP_TENANT_ID`) untouched — diff only adds lines after line 158, no modifications to existing code
- [x] Fail-closed: missing `github_login` → var not set (code verbs return empty, not crash) — `if github_login:` guard at line 164 ensures empty/None skips the export
- [x] Lane separation: Cognito for personal verbs, GitHub for code verbs only — code places the new var in its own block with clear comment, no routing changes
- [x] Chat-agent path limitation documented/deferred — PR description explicitly states "Out of scope"
- [x] `X-GitHub-Teams` deferred — not present in diff, explicitly noted as deferred
- [x] No Door-side changes needed — correct, not in diff
- [x] Unit tests: `ADP_GITHUB_LOGIN` set when present — `test_github_login_exported_when_present` ✓
- [x] Unit tests: `ADP_GITHUB_LOGIN` NOT set when absent — `test_github_login_not_set_when_absent` ✓
- [x] Regression: `ADP_OWNER_SUB` + `ADP_TENANT_ID` still set alongside — `test_cognito_identity_unchanged_after_github_login_added` ✓

## Invariant verification
- [x] "Do NOT touch / overwrite existing Cognito identity rail" — verified: no lines deleted/modified in `entrypoint.py`, only additions
- [x] "Never instead of them" — verified: `ADP_GITHUB_LOGIN` is set 5 lines AFTER `ADP_TENANT_ID`, no conditional relationship
- [x] "Graceful absence" — verified: empty string from `actor.get("github_login", "")` is falsy, skips the `if` block

## Findings (by confidence)

### HIGH — merge blockers
None.

### MEDIUM — discuss before merge
None.

### LOW — follow-up candidates
- The `SAMPLE_ENVELOPE` fixture already includes `github_login: "jane-dev"` — this is good for the positive test but means ALL existing tests that use the full `main()` path will now export `ADP_GITHUB_LOGIN` as a side effect. Not harmful (env vars are per-process and tests run in isolation), but worth noting for awareness.

## Test results
- 3 new tests: ALL PASS
- Full test file: 59 pass, 2 fail (pre-existing on main — VaultClient path prefix mismatch, unrelated)
- `ruff check`: PASS
- `ruff format --check`: PASS

## Recommendation
**APPROVE** — Clean, minimal, well-tested change that satisfies all acceptance criteria.
