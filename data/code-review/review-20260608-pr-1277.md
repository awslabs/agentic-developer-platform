# Review of PR #1277

## Driving issue
- #1275: gbrain Dockerfile: install pin v0.42.26.0 does not exist — fix install method until build SUCCEEDS

## Acceptance criteria checklist
- [x] **The build SUCCEEDS** — `adp-research-gbrain-build` reached `SUCCEEDED` (build `ebce075b-25eb-40c4-8cf2-7e600a982efd`, all phases green, image pushed to ECR at 18:34:44 UTC)
- [x] **Pin is reproducible** — pinned to commit SHA `959af1068d05f54477c70500fb8387a11f9ccb9c` (not a moving branch)
- [x] **No Docker Hub / no fictional tags** — no `oven/bun`, no `docker.io`, no `@v0.42.26.0`; only `public.ecr.aws/docker/library/node:22-slim`
- [x] **Uses #ref syntax** — `github:garrytan/gbrain#959af1068d...` (not the broken `@ref` form)
- [x] **Only Dockerfile changed** — single file modification (after agent_learning removal)
- [x] **Image runs gbrain** — build succeeded, image pushed `latest` tag to ECR (207MB); agent verified locally `gbrain --version` → `0.42.36.0` and `gbrain serve --help` works
- [x] **git added to apt deps** — required for git-based bun install, correctly added

## Findings (by confidence)

### HIGH — merge blockers
- ~~`agent_learning/2026-06-08-issue-1275-learnings.md` committed despite `agent_learning/` being in `.gitignore`~~ **FIXED**: removed in commit `b77db86` (reviewer pushed fix)

### MEDIUM — discuss before merge
- None

### LOW — follow-up candidates
- The single-stage build installs `git` (~40MB) only for the one-time `bun install` during build. A future optimization could use multi-stage properly (install in build stage, copy the entire `node_modules` dir — not just the symlink). Not blocking since the image works and is ~207MB total.
- The commit SHA pin (`959af1068d`) will never auto-update. If gbrain upstream ships security fixes, this pin must be manually bumped. Consider a comment in the Dockerfile noting when the pin was chosen and a reminder to periodically check for updates.

## Build verification
- **Build ID**: `adp-research-gbrain-build:ebce075b-25eb-40c4-8cf2-7e600a982efd`
- **Status**: SUCCEEDED
- **Duration**: ~64s total (BUILD phase: 23s)
- **ECR image**: `879318057152.dkr.ecr.us-east-1.amazonaws.com/adp-research-gbrain:latest` (pushed 2026-06-08T18:34:44Z)

## Recommendation
APPROVE — all acceptance criteria satisfied, build verified green, the one merge blocker (agent_learning file) has been fixed.
