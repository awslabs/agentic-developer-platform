name: Gateway Deploy

on:
  push:
    paths:
      - 'modules/gateway/src/**'
      - 'modules/gateway/Dockerfile'
      - 'modules/gateway/k8s/**'
      - 'modules/gateway/frontend/**'
      - '.github/workflows/gateway-deploy.yml'
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ENVIRONMENT: dev
  NAMESPACE: adp-gateway
  EKS_CLUSTER: adp-dev-eks-cluster
  ACCOUNT_ID: "879318057152"
  STATE_BUCKET: adp-terraform-state-879318057152

jobs:
  # Detect which parts of the gateway changed so we can skip the unrelated
  # build/deploy job on frontend-only or backend-only pushes. workflow_dispatch
  # always runs both (so humans can force a full redeploy).
  changes:
    name: Detect changes
    runs-on: arc-runner-org
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: filter
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "backend=true" >> "$GITHUB_OUTPUT"
            echo "frontend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # On push: diff the merge against its parent.
          CHANGED=$(git diff --name-only HEAD^ HEAD)
          echo "Changed files:"
          echo "$CHANGED"
          if echo "$CHANGED" | grep -qE '^(modules/gateway/src/|modules/gateway/Dockerfile|modules/gateway/k8s/)'; then
            echo "backend=true" >> "$GITHUB_OUTPUT"
          else
            echo "backend=false" >> "$GITHUB_OUTPUT"
          fi
          if echo "$CHANGED" | grep -qE '^modules/gateway/frontend/'; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
          else
            echo "frontend=false" >> "$GITHUB_OUTPUT"
          fi

  deploy-backend:
    name: Build and Deploy Backend
    runs-on: arc-runner-org
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    # Runner already has AWS creds via IRSA (adp-dev-agent-runner-role)

    steps:
      - uses: actions/checkout@v4

      - name: Package source and upload to S3
        run: |
          bash platform/scripts/zip-source.sh . /tmp/adp-source.zip
          aws s3 cp /tmp/adp-source.zip "s3://${{ env.STATE_BUCKET }}/codebuild/adp-source.zip"
          rm -f /tmp/adp-source.zip

      - name: Trigger image build in CodeBuild
        run: |
          REGISTRY="${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          BUILD_ID=$(aws codebuild start-build \
            --project-name adp-${{ env.ENVIRONMENT }}-gateway-build \
            --environment-variables-override \
              "name=AWS_REGION,value=${{ env.AWS_REGION }}" \
              "name=ENVIRONMENT,value=${{ env.ENVIRONMENT }}" \
              "name=ACCOUNT_ID,value=${{ env.ACCOUNT_ID }}" \
              "name=REGISTRY,value=${REGISTRY}" \
              "name=STATE_BUCKET,value=${{ env.STATE_BUCKET }}" \
              "name=EKS_CLUSTER,value=${{ env.EKS_CLUSTER }}" \
              "name=IMAGE_TAG,value=${{ github.sha }}" \
            --query 'build.id' --output text)
          echo "Started build: $BUILD_ID"
          while true; do
            STATUS=$(aws codebuild batch-get-builds --ids "$BUILD_ID" \
              --query 'builds[0].buildStatus' --output text)
            case $STATUS in
              SUCCEEDED) echo "Build succeeded"; break ;;
              FAILED|FAULT|STOPPED|TIMED_OUT)
                LOG_LINK=$(aws codebuild batch-get-builds --ids "$BUILD_ID" \
                  --query 'builds[0].logs.deepLink' --output text)
                echo "Build failed ($STATUS). Logs: $LOG_LINK"
                exit 1
                ;;
              *) sleep 10 ;;
            esac
          done
          echo "IMAGE_TAG=${{ github.sha }}" >> $GITHUB_ENV

      - name: Roll out new image
        run: |
          REGISTRY="${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER }} --region ${{ env.AWS_REGION }}
          kubectl set image deployment/bedrockgateway \
            bedrockgateway=${REGISTRY}/adp-gateway:${{ github.sha }} \
            -n ${{ env.NAMESPACE }}
          kubectl rollout status deployment/bedrockgateway -n ${{ env.NAMESPACE }} --timeout=300s

  deploy-frontend:
    name: Build and Deploy Frontend
    runs-on: arc-runner-org
    needs: [changes, deploy-backend]
    # Backend job is optional — run the frontend even when backend is skipped.
    # `always()` lets us bypass the default "cancelled if any dependency was
    # skipped" behavior; we still require the backend success if it did run.
    if: |
      always() &&
      needs.changes.outputs.frontend == 'true' &&
      (needs.deploy-backend.result == 'success' || needs.deploy-backend.result == 'skipped')

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Build frontend
        # Vite inlines these at build time. Without them, the SPA falls back
        # to stale literals in src/config/cognito.ts and shows "Authentication
        # not configured". These values map to the bedrockgw-dev-users pool
        # (see modules/gateway/infra/modules/cognito/) and the admin UI client.
        # TODO: publish these as SSM parameters from the cognito terraform
        # module and read them here, so pool rotation doesn't require a
        # workflow edit.
        env:
          VITE_API_URL: /api/gateway
          VITE_AGENT_WS_URL: wss://8ea7pg40b7.execute-api.us-east-1.amazonaws.com/v1
          VITE_COGNITO_USER_POOL_ID: us-east-1_JEhv9xSGG
          VITE_COGNITO_CLIENT_ID: 6cg7ba3hb4v41vbhm0cg8pl17j
          VITE_COGNITO_DOMAIN: bedrockgw-dev-auth-18057152
          VITE_COGNITO_REGION: us-east-1
        run: |
          cd modules/gateway/frontend
          npm ci
          npm run build

      - name: Deploy to S3 and invalidate CloudFront
        run: |
          BUCKET=$(aws ssm get-parameter \
            --name "/adp/${{ env.ENVIRONMENT }}/gateway/frontend-bucket" \
            --query "Parameter.Value" --output text)
          cd modules/gateway/frontend
          aws s3 sync dist/ "s3://${BUCKET}/" --delete

          DIST_ID=$(aws ssm get-parameter \
            --name "/adp/${{ env.ENVIRONMENT }}/gateway/cloudfront-id" \
            --query "Parameter.Value" --output text 2>/dev/null) || true
          if [ -n "$DIST_ID" ] && [ "$DIST_ID" != "None" ]; then
            aws cloudfront create-invalidation --distribution-id "$DIST_ID" --paths "/*"
          fi
