# Lambda API Authorizer Dependencies (Issue #239)
# These are packaged into a Lambda layer for the authorizer function.

# PyJWT with cryptography extras for RS256 JWT validation
PyJWT[crypto]>=2.9.0

# boto3 is provided by Lambda runtime but listed here for local testing
# boto3>=1.34.0

# requests is used for JWKS fetching (optional - can use urllib instead)
# requests>=2.31.0
