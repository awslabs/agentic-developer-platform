# E2E Test Findings — 2026-04-19 v2

**Issue:** #35 (re-run after #32 + #34 fixes)
**Tracker:** #36
**Previous findings:** #31

## Results Overview

| Suite | Total | Passed | Failed | Skipped | Status |
|-------|-------|--------|--------|---------|--------|
| pytest E2E | 97 | 97 | 0 | 6 deselected | all green |
| e2e_test.sh | 11 | 10 | 1 | 0 | 1 failure |

## Findings

| # | Title | Category | Severity | Source | Tracker |
|---|-------|----------|----------|--------|---------|
| 1 | `/api/admin/organizations` returns HTTP 500 via CloudFront | code | Medium | e2e_test.sh — Human CLI flow, Step 5 | #36 comment 1 |

## Finding 1: Admin organizations endpoint returns 500

**Category:** code
**Reproduction:** `GET /api/admin/organizations` with valid Cognito user JWT via CloudFront returns HTTP 500 (Internal Server Error from uvicorn).

**Evidence:**
- Response headers: `server: uvicorn`, `x-cache: Error from cloudfront`
- Health and proxy endpoints work fine through the same CloudFront path
- Via API Gateway: returns 403 (authorizer rejects user-pool JWT for admin routes)

**Impact:** Cannot validate admin endpoint through CloudFront in the bash E2E flow.

**Suggested fix:** Investigate backend logs for the unhandled exception on the admin route.
