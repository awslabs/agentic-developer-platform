name: "Call @agent-developer from ADP"

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  call-agent-developer:
    if: github.event.label.name == 'agent-developer'
    uses: aws-innovate/adp/.github/workflows/agent-developer.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repo_owner: ${{ github.repository_owner }}
      repo_name: ${{ github.event.repository.name }}
      target_repo: ${{ github.repository }}
    secrets: inherit
