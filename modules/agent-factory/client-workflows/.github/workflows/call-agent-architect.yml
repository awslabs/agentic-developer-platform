name: "Call @agent-architect from ADP"

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  call-agent-architect:
    if: github.event.label.name == 'agent-architect'
    uses: aws-innovate/adp/.github/workflows/agent-architect.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repo_owner: ${{ github.repository_owner }}
      repo_name: ${{ github.event.repository.name }}
      target_repo: ${{ github.repository }}
    secrets: inherit
