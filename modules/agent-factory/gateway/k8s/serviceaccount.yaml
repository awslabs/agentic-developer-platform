apiVersion: v1
kind: ServiceAccount
metadata:
  name: adp-agent
  namespace: adp-gateway-agents
  annotations:
    # Now managed by Terraform (modules/agent-factory/infra/gateway-main.tf).
    # This file is kept for reference / manual fallback only.
    # The role ARN is set automatically via the kubernetes_service_account resource.
    eks.amazonaws.com/role-arn: REPLACE_WITH_AGENT_WORKER_ROLE_ARN
  labels:
    app.kubernetes.io/name: adp-agent
    app.kubernetes.io/part-of: agent-gateway
    app.kubernetes.io/managed-by: terraform
