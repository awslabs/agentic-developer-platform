environment    = "dev"
account_id     = "879318057152"
aws_region     = "us-east-1"

# GraphRAG is expensive (Neptune Serverless + OpenSearch Serverless).
# Keep disabled unless explicitly needed.
graphrag_enabled = false
